# SeeMeTVC 对标 Tapnow Studio 补全技术方案文档

## 1. 文档说明

本文档基于 `需求文档.md`，将需求拆解为可实施的技术方案，用于指导 SeeMeTVC 后续迭代开发。

目标不是复制 [Tapnow Studio](https://github.com/OrangeBorning/tapnow-studio--) 的单文件应用形态，而是在 SeeMeTVC 现有架构上补齐关键能力：

- 智能视频反推。
- 多模态关键帧分析。
- scenes 自动展开为多镜头工作流。
- 图像生成计费与确认卡。
- 图像对比节点。
- 素材历史与版本管理。
- ASR 口播提取。
- 首尾帧视频控制。
- 更完整的模型能力矩阵。

最终形成一个 Agent 驱动、工作流可编辑、适合美妆 TVC 生产的平台化系统。

## 2. 当前架构基础

### 2.1 前端

当前前端核心模块：

```text
frontend/src/workflow/types.ts              工作流类型定义
frontend/src/workflow/ports.ts              节点端口定义
frontend/src/workflow/templates.ts          节点模板 / palette
frontend/src/workflow/WfInspector.tsx       节点参数检查器
frontend/src/workflow/canvas/MediaNode.tsx  节点渲染
```

当前已具备：

- React 工作流画布。
- 节点检查器。
- 图片 / 视频 / 音频 / 文本节点。
- LLM / 文生图 / 图生视频 / TTS / 视频处理节点。
- `VideoReversePrompt` 初版节点。

### 2.2 后端

当前后端核心模块：

```text
backend/app/services/workflow_exec.py      工作流执行器
backend/app/services/mcp_server.py         Agent 工具注册
backend/app/services/agent_runtime.py      Agent 循环
backend/app/services/image_gen.py          图像生成服务
backend/app/services/seedance.py           视频生成服务
backend/app/services/media_ops.py          ffmpeg 媒体操作
backend/app/services/video_reverse.py      视频反推服务
backend/app/node_contracts.yaml            节点契约
backend/app/api/uploads.py                 上传与本地文件解析
backend/app/bootstrap.py                   默认渠道初始化
```

当前已具备：

- Agent 可通过 MCP 工具修改画布。
- Agent 可调用节点执行工具。
- 工作流执行器可单节点运行。
- 图生视频已有确认卡和扣费链路。
- 图像生成已有 OpenAI-compatible 最小实现。
- 视频反推已有固定抽帧 + LLM 分析初版实现。

## 3. 总体技术路线

采用“增强现有节点执行器 + 增加少量专用服务 + 前端补齐交互”的路线。

总体流程：

```text
参考视频 / 产品图 / Brief
        │
        ▼
VideoReversePrompt 增强版
        │
        ├─ 智能场景检测
        ├─ 关键帧抽取
        ├─ 多模态视觉分析
        ├─ 可选 ASR 口播提取
        ▼
结构化 scenes
        │
        ▼
expand_scenes_to_nodes
        │
        ├─ TextAsset / LlmText
        ├─ TextToImage
        ├─ ImageCompare
        ├─ ImageToVideo / FirstLastFrameVideo
        ├─ TtsSpeak
        ├─ VideoMux / MixAudio / SubtitleBurn
        ▼
可运行多镜头 TVC 工作流
        │
        ▼
素材历史 / 版本管理 / 成本追踪
```

设计原则：

1. **不重写工作流引擎**：继续复用 `workflow_exec.py`。
2. **Agent 与手动画布共用同一套能力**：Agent 工具只是调用已有服务和节点。
3. **先做可闭环，再做多模型扩展**。
4. **节点输出必须结构化**：尤其是 `scenes`、`frames`、`timeline`。
5. **收费能力必须接入确认卡和退款机制**。
6. **模型差异通过 capability 描述，不在前端硬编码判断**。

## 4. 阶段一：视频反推增强方案

### 4.1 目标

将当前 `VideoReversePrompt` 从“固定抽帧 + 文本 LLM”升级为：

```text
智能场景检测
→ 关键帧提取
→ 多模态视觉分析
→ 结构化 scenes 输出
→ 可一键展开工作流
```

### 4.2 后端模块设计

新增或改造以下模块：

```text
backend/app/services/video_scene_detect.py     新增：视频场景检测
backend/app/services/vision_llm.py             新增：多模态 LLM 适配
backend/app/services/video_reverse.py          改造：编排场景检测和视觉分析
backend/app/services/media_ops.py              增强：抽帧、获取时长、转码辅助
backend/app/services/workflow_exec.py          增强：VideoReversePrompt 输出结构
```

### 4.3 智能场景检测实现

#### 4.3.1 输入

```python
video_url: str
max_scenes: int = 8
threshold: float = 0.28
sample_fps: float = 2.0
user_id: int | None
```

#### 4.3.2 输出

```python
class DetectedScene(BaseModel):
    index: int
    start_time: float
    end_time: float
    frame_path: str
    frame_url: str
    score: float
```

#### 4.3.3 推荐算法

第一版使用 ffmpeg + Python 图像差异：

1. 用 ffmpeg 按 `sample_fps` 抽临时帧。
2. 将帧缩放到小尺寸，例如 `160x90`。
3. 计算相邻帧平均绝对差。
4. 根据阈值识别 scene boundary。
5. 合并过短镜头。
6. 选每段中间帧作为关键帧。
7. 若检测结果少于 2 段，回退为等间隔抽帧。

差异分数示例：

```python
score = mean(abs(frame_i - frame_i_minus_1)) / 255.0
```

#### 4.3.4 文件保存

关键帧输出到：

```text
backend/uploads/{user_id}/video_reverse/{run_id}/scene_001.jpg
```

对外 URL：

```text
/uploads/{user_id}/video_reverse/{run_id}/scene_001.jpg
```

### 4.4 多模态视觉分析实现

#### 4.4.1 新增服务

```text
backend/app/services/vision_llm.py
```

职责：

- 选择支持视觉输入的 LLM Channel。
- 将本地图片转为 base64 data URL。
- 构造 OpenAI-compatible vision message。
- 预留 Anthropic / Gemini 消息构造。
- 输出严格 JSON。

#### 4.4.2 输入

```python
frames: list[DetectedScene]
style: Literal["seedance", "midjourney", "jimeng", "all"]
brief: str | None
channel_id: int | None
```

#### 4.4.3 输出

```python
class FrameAnalysis(BaseModel):
    index: int
    frame_url: str
    subject: str
    scene: str
    shot_size: str
    camera_motion: str
    lighting: str
    color_palette: str
    material: str
    action: str
    seedance_prompt: str
    midjourney_prompt: str | None = None
    jimeng_prompt: str | None = None
```

### 4.5 `VideoReversePrompt` 输出结构

建议在节点 `data` 中写入：

```json
{
  "kind": "prompt",
  "text": "整体视频分析...",
  "prompt": "可直接用于 Seedance 的总提示词...",
  "frames": ["/uploads/1/.../scene_001.jpg"],
  "timeline": [
    {
      "index": 1,
      "start_time": 0,
      "end_time": 3.2,
      "frame_url": "/uploads/1/.../scene_001.jpg",
      "score": 0.42
    }
  ],
  "scenes": [
    {
      "id": "scene_001",
      "title": "产品柔光开场",
      "start_time": 0,
      "end_time": 3.2,
      "frame_url": "/uploads/1/.../scene_001.jpg",
      "analysis": "...",
      "prompt": "...",
      "narration": "...",
      "negative_prompt": "不要字幕、水印、错字、额外 logo"
    }
  ]
}
```

### 4.6 前端改造

涉及文件：

```text
frontend/src/workflow/types.ts
frontend/src/workflow/ports.ts
frontend/src/workflow/templates.ts
frontend/src/workflow/WfInspector.tsx
frontend/src/workflow/canvas/MediaNode.tsx
```

新增 `VideoReversePrompt` 参数：

- `frame_strategy`: `fixed | scene_detect`
- `max_scenes`
- `scene_threshold`
- `sample_fps`
- `prompt_style`: `seedance | midjourney | jimeng | all`
- `vision_channel_id`

节点展示增强：

- 显示关键帧缩略图。
- 显示 scenes 数量。
- 显示每个镜头的起止时间。
- 提供“从分镜生成工作流”按钮。

## 5. 阶段二：scenes 自动展开工作流方案

### 5.1 目标

将 `VideoReversePrompt` 输出的 scenes 自动转换为多镜头节点链。

### 5.2 实现方式

优先实现为后端工具，供 Agent 和前端按钮共用。

新增服务：

```text
backend/app/services/scene_expand.py
```

新增 MCP 工具：

```text
expand_scenes_to_nodes
```

可选新增 API：

```text
POST /api/workflows/{workflow_id}/expand-scenes
```

### 5.3 输入参数

```json
{
  "source_node_id": "node_video_reverse_001",
  "mode": "silent",
  "create_images": true,
  "create_tts": false,
  "create_subtitles": false,
  "layout": "horizontal"
}
```

`mode` 可选：

- `silent`：只生成视频镜头链。
- `with_tts`：增加口播 TTS。
- `with_image`：每个 scene 先生成首帧图。
- `full_tvc`：首帧图、视频、TTS、拼接、字幕全生成。

### 5.4 输出节点结构

#### 5.4.1 silent 模式

```text
TextAsset(scene_001_prompt) → ImageToVideo(clip_001)
TextAsset(scene_002_prompt) → ImageToVideo(clip_002)
TextAsset(scene_003_prompt) → ImageToVideo(clip_003)
clip_001 + clip_002 + clip_003 → VideoMux
```

#### 5.4.2 with_image 模式

```text
TextAsset(scene_001_prompt) → TextToImage(first_frame_001) → ImageToVideo(clip_001)
TextAsset(scene_002_prompt) → TextToImage(first_frame_002) → ImageToVideo(clip_002)
TextAsset(scene_003_prompt) → TextToImage(first_frame_003) → ImageToVideo(clip_003)
clip_001 + clip_002 + clip_003 → VideoMux
```

#### 5.4.3 full_tvc 模式

```text
Scenes
→ TextAsset / LlmText
→ TextToImage
→ ImageToVideo
→ VideoMux
→ TtsSpeak
→ MixAudio
→ SubtitleBurn
```

### 5.5 布局规则

节点坐标以源节点为起点：

```text
source_node.x + 360 为第一列
每个 scene 占一行
横向间距 320
纵向间距 220
最终 VideoMux 放在最右侧居中
```

### 5.6 端口与连线

必须复用 `frontend/src/workflow/ports.ts` 和后端节点契约定义，避免 Agent 连出非法边。

建议新增后端端口校验函数：

```python
def validate_edge_by_contract(source_type, source_handle, target_type, target_handle) -> bool:
    ...
```

并在以下入口共用：

- Agent `connect` 工具。
- scene expand 服务。
- 工作流运行预检。

## 6. 阶段三：图像生成计费与确认卡方案

### 6.1 目标

让 `TextToImage` 与 `ImageToVideo` 一样接入确认、扣费和失败退款。

### 6.2 后端改造点

涉及文件：

```text
backend/app/services/workflow_exec.py
backend/app/services/image_gen.py
backend/app/services/mcp_server.py
backend/app/models.py 或相关计费模型
backend/app/api/workflows.py 或运行接口
```

### 6.3 扣费流程

```text
run_text_to_image
→ 读取 image channel
→ 计算 estimated_cost
→ 若 cost > 0 且未确认
    → 返回 pending_confirm
    → Agent / 前端展示确认卡
→ 用户确认
→ 扣除余额或冻结金额
→ 调用 image_gen.generate
→ 成功写入节点 url
→ 写入流水 succeeded
→ 失败退款并写入 failed
```

### 6.4 成本字段建议

在 `Channel` 或价格配置中支持：

```text
price_per_call
price_per_image
price_per_1k_tokens 可选
currency_unit
```

第一版可只支持：

```text
cost_per_run
```

### 6.5 确认卡事件

保持与视频生成一致，SSE 事件建议：

```json
{
  "type": "confirm_required",
  "run_id": "...",
  "node_id": "text_to_image_001",
  "node_type": "TextToImage",
  "cost": 1.0,
  "message": "本次图像生成预计消耗 1.0 点余额，是否继续？"
}
```

### 6.6 验收

- 免费 mock 图像不弹确认卡。
- 收费 OpenAI-compatible 图像弹确认卡。
- 用户取消不扣费。
- 成功生成扣费。
- 失败退款。
- Agent 调用时也能暂停等待确认。

## 7. 阶段四：ImageCompare 节点方案

### 7.1 目标

新增图像 AB 对比节点，服务于美妆修图、首帧选择和模型效果比较。

### 7.2 节点定义

节点类型：

```text
ImageCompare
```

输入端口：

```text
before: image
after: image
```

输出端口：

```text
image: image
```

节点数据：

```typescript
{
  kind: "image";
  before_url?: string;
  after_url?: string;
  selected?: "before" | "after";
  url?: string;
  compare_mode?: "slider" | "side_by_side";
}
```

### 7.3 前端实现

涉及文件：

```text
frontend/src/workflow/types.ts
frontend/src/workflow/ports.ts
frontend/src/workflow/templates.ts
frontend/src/workflow/WfInspector.tsx
frontend/src/workflow/canvas/MediaNode.tsx
```

新增组件建议：

```text
frontend/src/workflow/canvas/ImageCompareView.tsx
```

功能：

- 滑杆对比。
- 左右并排。
- 点击选择 before / after。
- 交换图片。
- 全屏预览。

### 7.4 后端执行逻辑

`ImageCompare` 本身不需要调用模型。

执行时：

1. 从上游解析 `before_url` 和 `after_url`。
2. 根据 `selected` 写入 `url`。
3. 输出 `kind=image`。

在 `workflow_exec.py` 中新增：

```python
def _exec_image_compare(...):
    ...
```

### 7.5 验收

- 可从两个 `ImageAsset` 或生成图节点接入。
- 节点内可滑动对比。
- 选择 A/B 后，输出图可接 `ImageToVideo`。

## 8. 阶段五：素材历史与版本管理方案

### 8.1 目标

将项目素材库升级为“生成历史 + 版本中心”。

### 8.2 数据模型

新增表建议：

```text
asset_versions
```

字段：

```text
id
user_id
project_id
workflow_id
node_id
node_type
kind
url
thumbnail_url
model_provider
model_name
channel_id
prompt
params_json
cost
status
error_message
favorite
created_at
updated_at
```

### 8.3 写入时机

以下节点成功执行后写入版本：

- `TextToImage`
- `ImageToVideo`
- `TtsSpeak`
- `VideoMux`
- `SubtitleBurn`
- `VideoReversePrompt` 关键帧
- `SpeechToText` 字幕 / 文本

### 8.4 API 设计

```text
GET    /api/projects/{project_id}/asset-versions
PATCH  /api/asset-versions/{id}
DELETE /api/asset-versions/{id}
POST   /api/asset-versions/bulk-delete
POST   /api/asset-versions/{id}/send-to-canvas
POST   /api/asset-versions/{id}/continue
```

查询参数：

```text
kind=image|video|audio|text
node_type=TextToImage
favorite=true|false
status=succeeded|failed
created_from=...
created_to=...
```

### 8.5 前端页面

可先在现有素材库增加“历史”Tab：

```text
素材库
├─ 上传素材
├─ 生成历史
└─ 收藏
```

第一版功能：

- 卡片列表。
- 按类型筛选。
- 收藏。
- 删除。
- 发送到画布。

### 8.6 验收

- 每次生成图像 / 视频后，历史中可见。
- 可将历史图像重新加入画布。
- 删除历史不影响已存在节点，或删除前明确提示。

## 9. 阶段六：ASR 口播提取方案

### 9.1 目标

从视频或音频中提取口播文本、时间轴 segments 和字幕。

### 9.2 节点类型

```text
SpeechToText
```

或：

```text
VideoTranscribe
```

建议第一版使用 `SpeechToText`，因为输入可同时支持音频和视频。

### 9.3 后端服务

新增：

```text
backend/app/services/asr.py
```

职责：

- 从视频中提取音频。
- 调用 ASR provider。
- 输出 transcript / segments / srt。

### 9.4 Provider 策略

第一版支持：

- local mock。
- OpenAI-compatible audio transcription。

预留：

- Whisper 本地。
- FunASR。
- 火山 / 阿里 / 腾讯 ASR。

### 9.5 节点输入输出

输入：

```text
media: audio | video
```

输出：

```text
text
segments
srt
```

节点数据：

```json
{
  "kind": "text",
  "text": "完整口播文本",
  "segments": [
    {
      "start": 0.0,
      "end": 2.3,
      "text": "第一句口播"
    }
  ],
  "srt": "1\n00:00:00,000 --> 00:00:02,300\n第一句口播\n"
}
```

### 9.6 工作流连接

```text
VideoAsset → SpeechToText → LlmText → TtsSpeak
SpeechToText → SubtitleBurn
```

### 9.7 验收

- 上传带口播视频后，能输出完整文本。
- 能输出可用于字幕烧录的 SRT。
- 能接 `LlmText` 改写口播。

## 10. 阶段七：首尾帧视频方案

### 10.1 目标

增强视频生成可控性，支持首帧、尾帧、风格图、产品图、角色图等参考输入。

### 10.2 实现方式

第一阶段不新增节点，先扩展 `ImageToVideo`：

新增字段：

```typescript
first_image_url?: string;
last_image_url?: string;
style_image_url?: string;
character_image_url?: string;
product_image_url?: string;
reference_strength?: number;
```

如果后续模型差异很大，再新增：

```text
FirstLastFrameVideo
```

### 10.3 Model Capability

新增模型能力描述：

```json
{
  "supports_image_to_video": true,
  "supports_text_to_video": true,
  "supports_first_last_frame": true,
  "supports_style_reference": false,
  "supports_character_reference": false,
  "supports_product_reference": true,
  "duration_min": 4,
  "duration_max": 10,
  "aspects": ["16:9", "9:16", "1:1"]
}
```

### 10.4 前端行为

- 根据当前 channel capability 显示可用输入项。
- 不支持的参数置灰或隐藏。
- 若用户连接了不支持的端口，运行前提示错误。

### 10.5 验收

- 支持首尾帧的模型可以提交首尾帧。
- 不支持首尾帧的模型不会错误提交。
- Agent 能根据模型能力选择合适节点链。

## 11. 模型能力矩阵方案

### 11.1 目标

统一描述不同模型和渠道能力，减少前端硬编码。

### 11.2 数据存储

可先存在 `Channel.config_json`：

```json
{
  "capabilities": {
    "kind": "image",
    "supports_text_to_image": true,
    "supports_image_to_image": true,
    "supports_multi_reference": false,
    "supports_negative_prompt": true,
    "supports_seed": true,
    "sizes": ["1024x1024", "1024x1792", "1792x1024"]
  }
}
```

后续可独立表：

```text
model_capabilities
```

### 11.3 API

```text
GET /api/channels/capabilities
GET /api/channels/{id}/capabilities
```

### 11.4 使用位置

- `WfInspector.tsx` 动态展示参数。
- `workflow_exec.py` 运行前校验。
- Agent Skill 提示中约束模型选择。
- scene expand 选择合适链路。

## 12. Agent 与 Skill 改造方案

### 12.1 `seedance-tvc` Skill 增强

补充以下行为：

1. 如果用户提供参考视频，优先创建 `VideoReversePrompt`。
2. 运行反推后，读取 scenes。
3. 调用 `expand_scenes_to_nodes` 展开多镜头工作流。
4. 若存在产品图，优先作为首帧 / 产品参考图。
5. 图像或视频收费时，等待确认卡。
6. 每步生成后总结可修改点。

### 12.2 MCP 工具新增

新增：

```text
expand_scenes_to_nodes
run_image_compare
run_speech_to_text
```

可选：

```text
get_node_output
list_asset_versions
send_asset_to_canvas
```

### 12.3 Agent 运行约束

Agent 需要遵循：

- 先问清 Brief，再搭图。
- 不直接跳过用户确认。
- 不把字幕、水印、法务文案交给视频模型生成。
- 不使用不支持的模型参数。
- scenes 超过 4 个时先询问用户是否压缩。

## 13. 前端整体改造清单

### 13.1 类型与节点

修改：

```text
frontend/src/workflow/types.ts
```

新增或扩展：

- `ImageCompare`
- `SpeechToText`
- `SceneExpand` 可选
- `FirstLastFrameVideo` 可选
- `timeline`
- `segments`
- `srt`
- `selected`
- `model_capabilities`

### 13.2 端口

修改：

```text
frontend/src/workflow/ports.ts
```

新增端口：

```text
ImageCompare.before: image
ImageCompare.after: image
ImageCompare.image: image
SpeechToText.media: video|audio
SpeechToText.text: text
SpeechToText.srt: text
```

### 13.3 节点模板

修改：

```text
frontend/src/workflow/templates.ts
```

新增 palette：

- 图像对比。
- 口播提取。
- 首尾帧视频，可选。

### 13.4 节点渲染

修改：

```text
frontend/src/workflow/canvas/MediaNode.tsx
```

新增展示：

- 关键帧网格。
- scenes 摘要。
- 图像滑杆对比。
- ASR 文本摘要。
- 运行进度。

### 13.5 检查器

修改：

```text
frontend/src/workflow/WfInspector.tsx
```

新增配置：

- `VideoReversePrompt` 智能检测参数。
- `TextToImage` 分辨率、seed、negative prompt 等。
- `ImageCompare` 模式和选中项。
- `SpeechToText` ASR provider。
- `ImageToVideo` 首尾帧参考。

## 14. 后端整体改造清单

### 14.1 新增服务

```text
backend/app/services/video_scene_detect.py
backend/app/services/vision_llm.py
backend/app/services/scene_expand.py
backend/app/services/asr.py
backend/app/services/asset_versions.py
backend/app/services/model_capabilities.py
```

### 14.2 修改服务

```text
backend/app/services/workflow_exec.py
backend/app/services/video_reverse.py
backend/app/services/image_gen.py
backend/app/services/mcp_server.py
backend/app/services/media_ops.py
backend/app/services/agent_runtime.py
backend/app/node_contracts.yaml
backend/app/bootstrap.py
```

### 14.3 新增或修改 API

```text
POST /api/workflows/{id}/expand-scenes
GET  /api/projects/{id}/asset-versions
POST /api/asset-versions/{id}/send-to-canvas
POST /api/asset-versions/bulk-delete
GET  /api/channels/{id}/capabilities
```

### 14.4 数据库迁移

建议新增表：

```text
asset_versions
```

可选新增表：

```text
model_capabilities
workflow_runs
node_runs
```

如果当前项目尚未使用 Alembic，建议先评估现有数据库初始化方式，再决定是否引入迁移工具。

## 15. 测试方案

### 15.1 单元测试

重点覆盖：

- 场景检测算法。
- scenes JSON 解析和容错。
- 多模态消息构造。
- scene expand 节点生成。
- 端口合法性校验。
- 图像扣费退款。
- ImageCompare 输出选择。

### 15.2 集成测试

测试链路：

```text
VideoAsset → VideoReversePrompt → expand_scenes_to_nodes → ImageToVideo → VideoMux
```

```text
TextToImage → ImageCompare → ImageToVideo
```

```text
VideoAsset → SpeechToText → SubtitleBurn
```

### 15.3 手工验收用例

#### 用例一：参考视频反推

1. 上传 15 秒美妆广告参考视频。
2. 创建 `VideoReversePrompt`。
3. 设置 `frame_strategy=scene_detect`。
4. 运行节点。
5. 查看关键帧和 scenes。

期望：

- 抽出 3–8 个关键帧。
- scenes 有起止时间。
- prompt 与视频内容相关。

#### 用例二：一键展开多镜头工作流

1. 在反推节点点击“从分镜生成工作流”。
2. 选择 `with_image`。
3. 查看画布。

期望：

- 每个 scene 生成 prompt 节点。
- 每个 scene 生成首帧图节点和视频节点。
- 自动连接到 `VideoMux`。

#### 用例三：图像收费确认

1. 配置收费图像 channel。
2. 运行 `TextToImage`。
3. 查看确认卡。
4. 确认后等待结果。

期望：

- 确认前不扣费。
- 成功后扣费。
- 失败后退款。

#### 用例四：图像对比

1. 上传两张图片。
2. 接入 `ImageCompare`。
3. 切换 slider / side by side。
4. 选择 after。
5. 接入 `ImageToVideo`。

期望：

- 对比正常。
- 输出为选择后的图片。

## 16. 风险与应对

### 16.1 多模态模型输出不稳定

风险：

- 模型不返回 JSON。
- 模型漏字段。
- 模型幻觉严重。

应对：

- 使用 strict JSON prompt。
- `_coerce_json` 容错。
- Pydantic 校验。
- 缺字段时 fallback。
- 保留原始模型输出用于排查。

### 16.2 场景检测效果不稳定

风险：

- 快速运动被误判为切镜。
- 缓慢转场无法识别。

应对：

- 提供阈值调节。
- 合并过短镜头。
- 限制最大 scenes。
- 保留等间隔 fallback。

### 16.3 成本和扣费链路复杂

风险：

- 重复扣费。
- 失败未退款。
- Agent 刷新后确认状态丢失。

应对：

- 复用视频确认卡机制。
- 每次 run 使用唯一 `run_id`。
- 流水幂等。
- pending 状态落库。

### 16.4 前端节点复杂度上升

风险：

- `MediaNode.tsx` 变得过重。

应对：

- 拆分专用组件：
  - `ImageCompareView.tsx`
  - `SceneTimelineView.tsx`
  - `FrameGridView.tsx`
  - `TranscriptPreview.tsx`

### 16.5 模型能力差异过大

风险：

- 不同 provider 参数完全不同。

应对：

- 通用字段 + provider-specific `params_json`。
- capability 控制 UI 展示。
- 后端运行前二次校验。

## 17. 推荐开发顺序

### Sprint 1：视频反推增强基础

- 新增 `video_scene_detect.py`。
- 增强 `media_ops.py` 抽帧能力。
- 改造 `video_reverse.py` 输出 timeline。
- 前端展示关键帧和 scenes。

### Sprint 2：多模态视觉分析

- 新增 `vision_llm.py`。
- 支持 OpenAI-compatible vision。
- `VideoReversePrompt` 增加 vision 参数。
- 完成 scenes 结构化输出。

### Sprint 3：scenes 展开工作流

- 新增 `scene_expand.py`。
- 新增 `expand_scenes_to_nodes` MCP 工具。
- 前端增加“从分镜生成工作流”按钮。
- 加端口合法性校验。

### Sprint 4：图像计费与 ImageCompare

- `TextToImage` 接入确认卡和扣费退款。
- 新增 `ImageCompare` 类型、端口、模板、渲染和执行逻辑。

### Sprint 5：素材历史

- 新增 `asset_versions` 表。
- 生成节点写入历史。
- 前端素材库增加历史 Tab。

### Sprint 6：ASR 与首尾帧

- 新增 `SpeechToText` 节点。
- 接入 OpenAI-compatible ASR 或 mock。
- 扩展 `ImageToVideo` 首尾帧参数。
- 增加模型能力矩阵。

## 18. 里程碑验收

### M1：参考视频可反推

验收：

```text
上传参考视频 → 自动切镜 → 多模态分析 → 输出 scenes
```

### M2：分镜可自动搭图

验收：

```text
scenes → 一键生成多镜头画布 → 预检通过
```

### M3：首帧图和视频闭环可计费

验收：

```text
TextToImage / ImageToVideo → 确认卡 → 扣费 → 失败退款
```

### M4：创作结果可管理

验收：

```text
生成图 / 视频 / 音频 → 自动进入历史 → 可收藏 / 复用 / 删除
```

### M5：参考视频口播可复用

验收：

```text
视频 → ASR → 文案改写 → TTS → 字幕烧录
```

## 19. 最终落地效果

完成本方案后，SeeMeTVC 应具备以下核心体验：

```text
用户上传产品图和参考视频
→ 选择 seedance-tvc Skill
→ Agent 自动分析参考视频
→ 生成结构化分镜
→ 一键搭建多镜头工作流
→ 生成首帧图并可 AB 对比
→ 确认扣费后生成视频片段
→ 自动拼接、配音、字幕
→ 成片输出并沉淀素材历史
```

届时 SeeMeTVC 与 Tapnow Studio 的关系将从“部分功能可参考”升级为：

```text
Tapnow Studio 偏轻量多模型创作工具箱。
SeeMeTVC 偏平台化、Agent 驱动、美妆 TVC 专业生产系统。
```
