# SeeMeTVC 对标 Tapnow Studio 补全开发任务清单

## 1. 文档说明

本文档基于 `设计文档.md` 拆解为可执行开发任务，用于后续迭代排期、开发跟踪和验收。

任务拆分原则：

- 先完成闭环，再扩展模型生态。
- 后端服务、节点执行、前端展示、Agent 工具同步推进。
- 每个任务尽量有明确输入、输出、涉及文件和验收标准。
- 所有新增能力必须同时支持手动画布和 Agent 调用。
- 涉及付费模型的任务必须纳入确认卡、扣费、失败退款。

## 2. 总体里程碑

| 里程碑 | 名称 | 目标 | 结果 |
| --- | --- | --- | --- |
| M1 | 视频反推增强 | 参考视频可智能切镜、多模态分析、输出 scenes | `VideoReversePrompt` 输出 `frames/timeline/scenes/prompt` |
| M2 | 分镜自动搭图 | scenes 可一键展开为多镜头工作流 | 自动生成 `TextAsset/TextToImage/ImageToVideo/VideoMux` 链路 |
| M3 | 图像生成闭环 | 图像生成支持确认、扣费、退款、参数面板 | `TextToImage` 达到视频节点同级生产能力 |
| M4 | 图像对比与选择 | 支持 AB 对比并选择结果继续生成 | 新增 `ImageCompare` 节点 |
| M5 | 素材历史 | 所有生成结果可沉淀、收藏、复用 | 新增 `asset_versions` 与历史素材 UI |
| M6 | 口播字幕链路 | 视频/音频可转文字和字幕 | 新增 `SpeechToText` 节点 |
| M7 | 模型能力矩阵 | 前后端按模型能力展示、校验参数 | 新增 capability 服务和 API |
| M8 | Agent 导演闭环 | `seedance-tvc` 能自动反推、搭图、确认、出片 | Agent 可完成美妆 TVC 多镜头工作流 |

## 3. 开发阶段总览

```text
阶段 1：视频反推增强
阶段 2：scenes 自动展开工作流
阶段 3：图像生成计费与参数增强
阶段 4：ImageCompare 图像对比节点
阶段 5：素材历史与版本管理
阶段 6：ASR 口播提取
阶段 7：首尾帧与模型能力矩阵
阶段 8：Agent / Skill 闭环增强
阶段 9：测试、验收与文档同步
```

## 4. 阶段 1：视频反推增强

目标：将当前 `VideoReversePrompt` 从固定抽帧升级为“智能场景检测 + 多模态关键帧分析 + 结构化 scenes 输出”。

### T1-01 新增视频场景检测服务

- 优先级：P0
- 类型：后端服务
- 依赖：无
- 涉及文件：
  - `backend/app/services/video_scene_detect.py` 新增
  - `backend/app/services/media_ops.py` 修改
  - `backend/requirements.txt` 可能修改

任务内容：

- 新增 `DetectedScene` 数据模型。
- 实现 `detect_scenes(...)`。
- 使用 ffmpeg 按 `sample_fps` 抽采样帧。
- 对采样帧缩放后计算相邻帧差异。
- 根据 `threshold` 识别镜头边界。
- 合并过短镜头。
- 选每个镜头中间帧作为关键帧。
- 保存最终关键帧到 `/uploads/{user_id}/video_reverse/{run_id}/`。
- 检测不到有效镜头时回退等间隔抽帧。

验收标准：

- 输入 10–30 秒视频，可输出 3–8 个关键帧。
- 输出包含 `index/start_time/end_time/frame_url/score`。
- 镜头切换明显的视频能提取不同镜头关键帧。
- ffmpeg 不存在时返回清晰错误。

### T1-02 增强媒体操作工具

- 优先级：P0
- 类型：后端服务
- 依赖：T1-01
- 涉及文件：
  - `backend/app/services/media_ops.py`

任务内容：

- 新增或增强获取视频时长方法。
- 新增按 fps 抽临时帧方法。
- 新增保存指定时间点关键帧方法。
- 抽帧临时目录按 `run_id` 隔离。
- 运行结束清理临时采样帧。

验收标准：

- 可从本地 upload 视频解析时长。
- 可按时间点导出 jpg/png 关键帧。
- 临时文件不会污染 uploads 根目录。

### T1-03 新增多模态视觉 LLM 服务

- 优先级：P0
- 类型：后端服务
- 依赖：T1-01
- 涉及文件：
  - `backend/app/services/vision_llm.py` 新增
  - `backend/app/services/llm.py` 或现有 LLM 调用模块，视项目结构调整

任务内容：

- 新增 `FrameAnalysis` 数据模型。
- 实现 `analyze_video_frames(...)`。
- 支持 OpenAI-compatible vision 消息格式。
- 支持本地图片转 base64 data URL。
- 支持用户指定 `vision_channel_id`。
- 自动选择具备 `vision=true` capability 的 LLM 渠道。
- 模型无视觉能力时回退普通 LLM 或本地模拟。
- 输出严格 JSON，并进行容错解析。

验收标准：

- 给定关键帧，输出 `subject/scene/shot_size/camera_motion/lighting/color_palette/material/action/seedance_prompt`。
- 非 JSON 输出可被容错处理。
- 无视觉渠道时不会中断整个反推流程。

### T1-04 改造视频反推编排服务

- 优先级：P0
- 类型：后端服务
- 依赖：T1-01、T1-03
- 涉及文件：
  - `backend/app/services/video_reverse.py`

任务内容：

- 扩展 `reverse_prompt(...)` 入参：
  - `frame_strategy`
  - `frame_count`
  - `max_scenes`
  - `scene_threshold`
  - `sample_fps`
  - `prompt_style`
  - `vision_channel_id`
- 编排 `video_scene_detect.detect_scenes`。
- 编排 `vision_llm.analyze_video_frames`。
- 生成统一 `VideoReverseResult`。
- 输出 `text/prompt/frames/timeline/scenes/raw`。
- 保留 fallback 分析逻辑。

验收标准：

- `frame_strategy=scene_detect` 时使用智能切镜。
- `frame_strategy=fixed` 时保留旧抽帧逻辑。
- 输出 scenes 可被 JSON 序列化并写入节点 data。

### T1-05 改造工作流执行器中的 VideoReversePrompt

- 优先级：P0
- 类型：后端执行器
- 依赖：T1-04
- 涉及文件：
  - `backend/app/services/workflow_exec.py`
  - `backend/app/node_contracts.yaml`

任务内容：

- 增强 `_exec_video_reverse`。
- 解析上游 `VideoAsset` 和可选 `TextAsset` brief。
- 将节点 data 参数传入 `video_reverse.reverse_prompt`。
- 将 `frames/timeline/scenes/prompt/text` 写回节点 data。
- `_has_usable_output` 支持 `scenes`。
- `_tag_ports` 支持 `scenes/frames/timeline`。
- 更新 `node_contracts.yaml` 中 `VideoReversePrompt` 输入输出说明。

验收标准：

- 手动运行 `VideoReversePrompt` 可得到结构化 scenes。
- Agent 调用 `run_video_reverse_prompt` 可得到相同结果。
- 下游节点可读取 `prompt` 或 `scenes`。

### T1-06 前端增强 VideoReversePrompt 参数与展示

- 优先级：P0
- 类型：前端
- 依赖：T1-05
- 涉及文件：
  - `frontend/src/workflow/types.ts`
  - `frontend/src/workflow/ports.ts`
  - `frontend/src/workflow/templates.ts`
  - `frontend/src/workflow/WfInspector.tsx`
  - `frontend/src/workflow/canvas/MediaNode.tsx`
  - `frontend/src/workflow/canvas/FrameGridView.tsx` 新增
  - `frontend/src/workflow/canvas/SceneTimelineView.tsx` 新增

任务内容：

- 新增 `Scene`、`TimelineItem` 类型。
- 扩展 `WfData`：`scenes/timeline/sample_fps/max_scenes/scene_threshold/prompt_style/vision_channel_id`。
- 检查器新增智能检测参数。
- 节点卡片展示关键帧缩略图。
- 节点卡片展示 scenes 数量和时间轴摘要。
- 增加“从分镜生成工作流”按钮入口，先可置灰或提示待实现。

验收标准：

- 运行完成后前端可查看关键帧。
- 可查看 scene 标题、起止时间和 prompt 摘要。
- 修改参数后保存到 graph_json。

## 5. 阶段 2：scenes 自动展开工作流

目标：将 `VideoReversePrompt` 输出的 scenes 自动转换为多镜头节点链。

### T2-01 新增场景展开服务

- 优先级：P0
- 类型：后端服务
- 依赖：T1-05
- 涉及文件：
  - `backend/app/services/scene_expand.py` 新增
  - `backend/app/services/graph_ops.py` 可能修改

任务内容：

- 实现 `expand_scenes_to_nodes(...)`。
- 从指定 `source_node_id` 读取 scenes。
- 支持 `silent/with_image/with_tts/full_tvc` 模式。
- 生成 `TextAsset`、`TextToImage`、`ImageToVideo`、`VideoMux` 等节点。
- 按布局规则计算节点坐标。
- 生成合法 edges。
- 返回新增节点和边 ID。

验收标准：

- 3 个 scenes 可展开为 3 条视频镜头链。
- `with_image` 模式下每条链包含 `TextToImage`。
- 最终 clips 自动接入 `VideoMux`。

### T2-02 新增端口契约校验

- 优先级：P0
- 类型：后端执行器 / 图工具
- 依赖：T2-01
- 涉及文件：
  - `backend/app/services/workflow_exec.py`
  - `backend/app/services/graph_ops.py`
  - `backend/app/node_contracts.yaml`

任务内容：

- 新增 `validate_edge_by_contract(...)`。
- 校验 source/target 节点类型。
- 校验 sourceHandle/targetHandle 是否存在。
- 校验 kind 是否兼容。
- 在 Agent `connect` 工具中调用。
- 在 `scene_expand.py` 中调用。
- 在工作流运行预检中调用。

验收标准：

- Agent 不能连出前端不支持的非法边。
- 自动展开服务生成的边全部通过校验。
- 非法连接返回明确错误。

### T2-03 新增场景展开 API

- 优先级：P0
- 类型：后端 API
- 依赖：T2-01、T2-02
- 涉及文件：
  - `backend/app/api/workflows.py` 或现有 workflow API 文件

任务内容：

- 新增 `POST /api/workflows/{workflow_id}/expand-scenes`。
- 校验当前用户项目权限。
- 调用 `scene_expand.expand_scenes_to_nodes`。
- 保存更新后的 `graph_json`。
- 返回 `created_node_ids/created_edge_ids/graph_json`。

验收标准：

- 前端按钮可调用 API 成功展开。
- 刷新页面后新节点仍存在。

### T2-04 新增 MCP 工具 expand_scenes_to_nodes

- 优先级：P0
- 类型：Agent / MCP
- 依赖：T2-01、T2-02
- 涉及文件：
  - `backend/app/services/mcp_server.py`
  - `backend/app/services/agent_runtime.py` 可能修改

任务内容：

- 在 MCP 工具列表注册 `expand_scenes_to_nodes`。
- 工具入参包含 `source_node_id/mode/create_images/create_tts/create_subtitles`。
- 工具调用场景展开服务并写回 graph。
- SSE 推送画布更新。

验收标准：

- Agent 可在反推后调用该工具自动搭图。
- 工具返回新增节点和边 ID。

### T2-05 前端接入“从分镜生成工作流”按钮

- 优先级：P0
- 类型：前端
- 依赖：T2-03
- 涉及文件：
  - `frontend/src/workflow/WfInspector.tsx`
  - 前端 API client 文件，视项目结构调整

任务内容：

- 在 `VideoReversePrompt` 检查器中新增按钮。
- 支持选择展开模式：`silent/with_image/with_tts/full_tvc`。
- 调用后端 API。
- 成功后刷新画布。
- 展示新增节点数量。

验收标准：

- 用户点击按钮后可看到自动生成的多镜头链路。
- 无 scenes 时按钮置灰或提示先运行反推节点。

## 6. 阶段 3：图像生成计费与参数增强

目标：让 `TextToImage` 成为可计费、可确认、可配置参数的真实生产节点。

### T3-01 梳理并复用视频确认卡机制

- 优先级：P0
- 类型：后端计费
- 依赖：无
- 涉及文件：
  - `backend/app/services/workflow_exec.py`
  - 现有余额 / 流水 / 确认卡相关文件

任务内容：

- 梳理 `ImageToVideo` 当前确认卡流程。
- 抽出通用确认卡方法。
- 统一 `run_id/node_id/estimated_cost/status`。
- 确保 pending 状态可落库或刷新恢复。

验收标准：

- 不破坏现有视频确认卡。
- 后续 `TextToImage` 可复用同一套流程。

### T3-02 TextToImage 接入确认卡、扣费、退款

- 优先级：P0
- 类型：后端计费 / 节点执行
- 依赖：T3-01
- 涉及文件：
  - `backend/app/services/workflow_exec.py`
  - `backend/app/services/image_gen.py`
  - 余额流水相关模型 / 服务

任务内容：

- 为 image channel 增加成本读取逻辑。
- `_exec_t2i` 运行前估算成本。
- 成本大于 0 且未确认时返回确认卡。
- 用户确认后扣费。
- 调用 `image_gen.generate`。
- 成功写节点输出。
- 失败自动退款。
- 本地模拟不扣费。

验收标准：

- 收费图像渠道弹确认卡。
- 用户取消不扣费。
- 成功生成扣费。
- 上游失败退款。
- Agent 调用也能暂停等待确认。

### T3-03 增强 TextToImage 参数

- 优先级：P1
- 类型：前后端
- 依赖：T3-02
- 涉及文件：
  - `frontend/src/workflow/types.ts`
  - `frontend/src/workflow/WfInspector.tsx`
  - `backend/app/services/image_gen.py`
  - `backend/app/services/workflow_exec.py`

任务内容：

- 增加参数：
  - `size`
  - `negative_prompt`
  - `seed`
  - `batch_size`
  - `image_strength`
- 后端将支持的参数传给 provider。
- 不支持的参数根据 capability 置灰或忽略。

验收标准：

- 检查器可编辑图像参数。
- OpenAI-compatible 默认至少支持 size。
- 不支持参数不会导致请求失败。

### T3-04 图像生成结果写入素材历史

- 优先级：P1
- 类型：后端历史
- 依赖：T3-02、T5-01
- 涉及文件：
  - `backend/app/services/workflow_exec.py`
  - `backend/app/services/asset_versions.py`

任务内容：

- `TextToImage` 成功后记录 asset version。
- 记录 prompt、channel、model、cost、params。

验收标准：

- 每次图像生成成功后可在历史中查询。

## 7. 阶段 4：ImageCompare 图像对比节点

目标：新增 AB 图像对比节点，支持选择结果继续下游生成。

### T4-01 前端类型、端口、模板新增 ImageCompare

- 优先级：P0
- 类型：前端
- 依赖：无
- 涉及文件：
  - `frontend/src/workflow/types.ts`
  - `frontend/src/workflow/ports.ts`
  - `frontend/src/workflow/templates.ts`

任务内容：

- `WfNodeType` 增加 `ImageCompare`。
- `WfData` 增加 `before_url/after_url/selected/compare_mode`。
- ports 增加 `before/after/image`。
- palette 增加“图像对比”。
- defaultData 设置 `compare_mode=slider`、`selected=after`。

验收标准：

- 画布可创建 `ImageCompare`。
- 可连接两个 image 输入。

### T4-02 实现 ImageCompareView

- 优先级：P0
- 类型：前端组件
- 依赖：T4-01
- 涉及文件：
  - `frontend/src/workflow/canvas/ImageCompareView.tsx` 新增
  - `frontend/src/workflow/canvas/MediaNode.tsx`

任务内容：

- 实现滑杆对比模式。
- 实现左右并排模式。
- 支持选择 before 或 after。
- 支持缺图占位提示。
- 可选支持全屏预览。

验收标准：

- 两张图接入后可滑动对比。
- 选择状态可保存到节点 data。

### T4-03 检查器支持 ImageCompare

- 优先级：P0
- 类型：前端
- 依赖：T4-02
- 涉及文件：
  - `frontend/src/workflow/WfInspector.tsx`

任务内容：

- 显示 before / after 预览。
- 切换 compare mode。
- 切换 selected。
- 支持交换 before / after。

验收标准：

- 用户可在检查器里切换输出选择。

### T4-04 后端执行器支持 ImageCompare

- 优先级：P0
- 类型：后端执行器
- 依赖：T4-01
- 涉及文件：
  - `backend/app/services/workflow_exec.py`
  - `backend/app/node_contracts.yaml`
  - `backend/app/services/mcp_server.py`

任务内容：

- `NODE_TYPES` 增加 `ImageCompare`。
- `PRODUCER_TYPES` 增加 `ImageCompare`。
- 新增 `_exec_image_compare`。
- `_has_usable_output` 判断 `data.url`。
- `_tag_ports` 输出 image。
- MCP 注册 `run_image_compare`。
- node contracts 增加节点说明。

验收标准：

- 运行节点后 `data.url` 等于选中的图片。
- 输出可连接到 `ImageToVideo`。
- Agent 可运行该节点。

## 8. 阶段 5：素材历史与版本管理

目标：生成结果可沉淀为历史资产，并可收藏、删除、复用。

### T5-01 新增 asset_versions 数据模型

- 优先级：P1
- 类型：数据库 / 后端模型
- 依赖：无
- 涉及文件：
  - `backend/app/models.py` 或模型目录
  - 数据库初始化 / 迁移文件

任务内容：

- 新增 `AssetVersion` 模型。
- 字段包含：`user_id/project_id/workflow_id/node_id/node_type/kind/url/thumbnail_url/text/model_provider/model_name/channel_id/prompt/params_json/cost/status/error_message/favorite/created_at/updated_at`。
- 增加常用索引。

验收标准：

- 后端启动后表存在。
- 可创建、查询、更新、删除记录。

### T5-02 新增素材版本服务

- 优先级：P1
- 类型：后端服务
- 依赖：T5-01
- 涉及文件：
  - `backend/app/services/asset_versions.py` 新增

任务内容：

- 实现 `record_asset_version(...)`。
- 实现列表查询。
- 实现收藏更新。
- 实现批量删除。
- 实现发送到画布辅助方法。

验收标准：

- 节点执行后可统一调用记录服务。
- 查询支持 kind、node_type、favorite、status 过滤。

### T5-03 新增素材历史 API

- 优先级：P1
- 类型：后端 API
- 依赖：T5-02
- 涉及文件：
  - `backend/app/api/asset_versions.py` 新增
  - `backend/app/main.py` 或路由注册文件

任务内容：

- `GET /api/projects/{project_id}/asset-versions`
- `PATCH /api/asset-versions/{id}`
- `POST /api/asset-versions/bulk-delete`
- `POST /api/asset-versions/{id}/send-to-canvas`
- 权限校验当前用户只能访问自己的项目资产。

验收标准：

- 前端可分页查询历史。
- 可收藏 / 取消收藏。
- 可批量删除记录。
- 可把历史资产发送到画布。

### T5-04 节点执行成功后写入历史

- 优先级：P1
- 类型：后端执行器
- 依赖：T5-02
- 涉及文件：
  - `backend/app/services/workflow_exec.py`

任务内容：

- 以下节点成功后记录历史：
  - `TextToImage`
  - `ImageToVideo`
  - `TtsSpeak`
  - `VideoMux`
  - `SubtitleBurn`
  - `VideoReversePrompt`
  - `SpeechToText`
- 失败可选记录 `status=failed`。

验收标准：

- 生成图 / 视频 / 音频 / 文本可进入历史。
- 记录中包含来源节点和模型信息。

### T5-05 前端素材库新增历史 Tab

- 优先级：P1
- 类型：前端
- 依赖：T5-03
- 涉及文件：
  - 前端素材库相关组件，需根据现有结构定位
  - 前端 API client 文件

任务内容：

- 素材库增加“生成历史”Tab。
- 卡片展示缩略图 / 类型 / 来源节点 / 时间。
- 支持 kind 筛选。
- 支持收藏。
- 支持删除。
- 支持发送到画布。

验收标准：

- 用户可查看项目内生成历史。
- 历史图片可重新加入画布成为 `ImageAsset`。

## 9. 阶段 6：ASR 口播提取

目标：新增 `SpeechToText` 节点，从视频或音频生成 transcript、segments 和 srt。

### T6-01 新增 ASR 服务

- 优先级：P1
- 类型：后端服务
- 依赖：无
- 涉及文件：
  - `backend/app/services/asr.py` 新增
  - `backend/app/services/media_ops.py` 修改

任务内容：

- 新增 `TranscribeResult` 和 `TranscriptSegment`。
- 实现 `transcribe(...)`。
- 如果输入为视频，先提取音频。
- 支持 local mock。
- 支持 OpenAI-compatible transcription。
- 输出 text、segments、srt。

验收标准：

- 输入音频可得到 mock transcript。
- 配置真实 ASR channel 后可得到真实 transcript。
- segments 可转换为 SRT。

### T6-02 前端新增 SpeechToText 类型、端口、模板

- 优先级：P1
- 类型：前端
- 依赖：T6-01
- 涉及文件：
  - `frontend/src/workflow/types.ts`
  - `frontend/src/workflow/ports.ts`
  - `frontend/src/workflow/templates.ts`

任务内容：

- `WfNodeType` 增加 `SpeechToText`。
- `WfData` 增加 `segments/srt/language/media_url`。
- ports 增加 `media/audio/text/srt`。
- palette 增加“口播提取”。

验收标准：

- 可创建 `SpeechToText` 节点。
- 可连接 `VideoAsset` 或 `AudioAsset`。

### T6-03 后端执行器支持 SpeechToText

- 优先级：P1
- 类型：后端执行器
- 依赖：T6-01、T6-02
- 涉及文件：
  - `backend/app/services/workflow_exec.py`
  - `backend/app/node_contracts.yaml`
  - `backend/app/services/mcp_server.py`

任务内容：

- 新增 `_exec_speech_to_text`。
- 从上游读取 video/audio URL。
- 调用 `asr.transcribe`。
- 写入 `text/segments/srt`。
- `_tag_ports` 支持 text 和 srt。
- MCP 注册 `run_speech_to_text`。

验收标准：

- 运行节点可得到文本和 SRT。
- 输出可接 `SubtitleBurn` 或 `LlmText`。

### T6-04 前端展示和检查器支持 SpeechToText

- 优先级：P1
- 类型：前端
- 依赖：T6-03
- 涉及文件：
  - `frontend/src/workflow/WfInspector.tsx`
  - `frontend/src/workflow/canvas/MediaNode.tsx`
  - `frontend/src/workflow/canvas/TranscriptPreview.tsx` 新增

任务内容：

- 节点卡片展示 transcript 摘要。
- 检查器展示完整文本。
- 检查器展示 segments 或 SRT。
- 支持复制字幕文本。

验收标准：

- 用户可查看并复制 ASR 结果。

## 10. 阶段 7：首尾帧与模型能力矩阵

目标：增强视频可控性，并让前后端根据模型能力动态展示和校验参数。

### T7-01 新增模型能力服务

- 优先级：P1
- 类型：后端服务
- 依赖：无
- 涉及文件：
  - `backend/app/services/model_capabilities.py` 新增
  - Channel 模型 / 配置相关文件

任务内容：

- 定义 `ModelCapabilities`。
- 从 `Channel.config_json.capabilities` 读取能力。
- provider 默认能力 fallback。
- 实现 `get_channel_capabilities(...)`。
- 实现 `validate_node_params_against_capabilities(...)`。

验收标准：

- OpenAI-compatible image 返回 image 默认能力。
- Seedance video 返回 video 默认能力。
- 不支持参数能输出明确错误。

### T7-02 新增 Channel Capability API

- 优先级：P1
- 类型：后端 API
- 依赖：T7-01
- 涉及文件：
  - Channel API 相关文件

任务内容：

- 新增 `GET /api/channels/{id}/capabilities`。
- 可选新增 `GET /api/channels/capabilities`。
- 返回 provider、model、kind、capabilities。

验收标准：

- 前端可读取当前 channel 支持能力。

### T7-03 ImageToVideo 增加首尾帧字段

- 优先级：P1
- 类型：前后端
- 依赖：T7-01
- 涉及文件：
  - `frontend/src/workflow/types.ts`
  - `frontend/src/workflow/WfInspector.tsx`
  - `backend/app/services/workflow_exec.py`
  - `backend/app/services/seedance.py` 或视频 provider 文件

任务内容：

- `WfData` 增加：
  - `first_image_url`
  - `last_image_url`
  - `style_image_url`
  - `character_image_url`
  - `product_image_url`
  - `reference_strength`
- 检查器支持选择参考图。
- 后端运行前校验 capability。
- Provider 支持时传递对应参数。
- Provider 不支持时阻止运行并提示。

验收标准：

- 支持首尾帧的模型可提交首尾帧。
- 不支持首尾帧的模型给出明确错误。

### T7-04 TextToImage 参数按 capability 动态展示

- 优先级：P1
- 类型：前端
- 依赖：T7-02、T3-03
- 涉及文件：
  - `frontend/src/workflow/WfInspector.tsx`
  - 前端 Channel API client

任务内容：

- 检查器加载当前 image channel capability。
- 根据 capability 显示或隐藏 size、seed、negative prompt、image strength 等。
- 不支持字段置灰并提示原因。

验收标准：

- 更换模型后参数面板随能力变化。

## 11. 阶段 8：Agent / Skill 闭环增强

目标：让 `seedance-tvc` Skill 能利用新增节点完成从参考视频到多镜头 TVC 的自动搭图。

### T8-01 增强 seedance-tvc Skill

- 优先级：P0
- 类型：Agent Skill
- 依赖：T1、T2、T3
- 涉及文件：
  - `backend/app/skills/seedance-tvc/SKILL.md`

任务内容：

- 明确有参考视频时优先创建 `VideoReversePrompt`。
- 运行反推后读取 scenes。
- scenes 数量 1–4 时调用 `expand_scenes_to_nodes`。
- scenes 超过 4 时询问用户是否压缩。
- 有产品图时作为首帧 / 产品参考图。
- 提醒避免视频模型生成字幕、水印、法务文案。
- 付费节点必须等待用户确认。

验收标准：

- 真 LLM 下 Agent 能按 Skill 自动规划并搭图。
- 不会绕过确认卡。

### T8-02 MCP 工具清单补齐

- 优先级：P0
- 类型：Agent / MCP
- 依赖：T2-04、T4-04、T6-03
- 涉及文件：
  - `backend/app/services/mcp_server.py`

任务内容：

- 注册：
  - `expand_scenes_to_nodes`
  - `run_image_compare`
  - `run_speech_to_text`
- 可选注册：
  - `get_node_output`
  - `list_asset_versions`
  - `send_asset_to_canvas`

验收标准：

- Agent 可发现并调用新增工具。
- 工具错误会以可理解文本返回。

### T8-03 Agent 输出过程优化

- 优先级：P1
- 类型：Agent Runtime / SSE
- 依赖：T8-02
- 涉及文件：
  - `backend/app/services/agent_runtime.py`

任务内容：

- Agent 在关键步骤输出简短过程说明。
- 运行反推、展开分镜、生成图像、生成视频时推送工具状态。
- 遇到确认卡时清晰说明等待用户确认。

验收标准：

- 用户能看懂 Agent 当前在做什么。
- 长任务不显得“卡死”。

## 12. 阶段 9：测试、验收与文档同步

目标：保证新增功能可稳定运行，并同步更新文档。

### T9-01 单元测试：视频场景检测

- 优先级：P1
- 类型：测试
- 依赖：T1-01
- 涉及文件：
  - 后端测试目录，按现有结构新增

任务内容：

- 测试帧差计算。
- 测试边界识别。
- 测试短镜头合并。
- 测试 fallback 等间隔抽帧。

验收标准：

- 场景检测核心函数有自动化测试覆盖。

### T9-02 单元测试：scene_expand

- 优先级：P1
- 类型：测试
- 依赖：T2-01

任务内容：

- 输入 3 个 scenes。
- 验证生成节点类型和数量。
- 验证生成边合法。
- 验证布局坐标非重叠。

验收标准：

- 自动展开服务测试通过。

### T9-03 单元测试：端口校验

- 优先级：P1
- 类型：测试
- 依赖：T2-02

任务内容：

- 测试合法 image → image 输入。
- 测试非法 video → image 输入。
- 测试缺 handle。
- 测试未知节点类型。

验收标准：

- Agent 和自动展开不能生成非法边。

### T9-04 集成测试：参考视频到多镜头画布

- 优先级：P1
- 类型：测试 / 手工验收
- 依赖：T1、T2

流程：

```text
VideoAsset → VideoReversePrompt → expand_scenes_to_nodes → ImageToVideo → VideoMux
```

验收标准：

- 可从参考视频得到多镜头节点链。
- 自动连线正确。
- 预检通过。

### T9-05 集成测试：图像生成与对比

- 优先级：P1
- 类型：测试 / 手工验收
- 依赖：T3、T4

流程：

```text
TextToImage → ImageCompare → ImageToVideo
```

验收标准：

- 图像生成成功。
- 可 AB 对比。
- 选择后的图可用于图生视频。

### T9-06 集成测试：口播提取到字幕

- 优先级：P1
- 类型：测试 / 手工验收
- 依赖：T6

流程：

```text
VideoAsset → SpeechToText → SubtitleBurn
```

验收标准：

- 可生成 transcript 和 srt。
- srt 可用于字幕烧录。

### T9-07 更新 README 与三份规划文档

- 优先级：P2
- 类型：文档
- 依赖：主要功能完成后
- 涉及文件：
  - `README.md`
  - `需求文档.md`
  - `方案文档.md`
  - `设计文档.md`
  - `任务清单.md`

任务内容：

- 将已完成能力从“计划”移动到“已具备”。
- 更新运行说明。
- 更新限制说明。
- 更新后续路线。

验收标准：

- 文档与实际功能一致。

## 13. 任务依赖关系

```text
T1-01 → T1-02 → T1-04 → T1-05 → T1-06
T1-01 → T1-03 → T1-04
T1-05 → T2-01 → T2-03 → T2-05
T2-01 → T2-02 → T2-04
T3-01 → T3-02 → T3-03
T5-01 → T5-02 → T5-03 → T5-05
T5-02 → T3-04 / T5-04
T6-01 → T6-02 → T6-03 → T6-04
T7-01 → T7-02 → T7-03 / T7-04
T1 + T2 + T3 → T8-01
T2-04 + T4-04 + T6-03 → T8-02
```

## 14. 推荐开发顺序

### 第一轮：P0 闭环

1. T1-01 视频场景检测服务。
2. T1-02 媒体操作增强。
3. T1-03 多模态视觉 LLM 服务。
4. T1-04 视频反推编排改造。
5. T1-05 工作流执行器支持增强反推。
6. T1-06 前端展示关键帧和 scenes。
7. T2-01 场景展开服务。
8. T2-02 端口契约校验。
9. T2-03 场景展开 API。
10. T2-04 MCP 工具。
11. T2-05 前端按钮。
12. T3-01 复用确认卡机制。
13. T3-02 TextToImage 计费。
14. T4-01 ImageCompare 基础类型。
15. T4-02 ImageCompare 视图。
16. T4-03 ImageCompare 检查器。
17. T4-04 ImageCompare 执行器。
18. T8-01 增强 Skill。
19. T8-02 MCP 工具补齐。

### 第二轮：P1 生产化

1. T3-03 TextToImage 参数增强。
2. T5-01 asset_versions 模型。
3. T5-02 asset_versions 服务。
4. T5-03 素材历史 API。
5. T5-04 节点结果写历史。
6. T5-05 前端历史 Tab。
7. T6-01 ASR 服务。
8. T6-02 SpeechToText 前端节点。
9. T6-03 SpeechToText 执行器。
10. T6-04 SpeechToText 展示。
11. T7-01 模型能力服务。
12. T7-02 Capability API。
13. T7-03 ImageToVideo 首尾帧。
14. T7-04 TextToImage 动态参数。
15. T8-03 Agent 输出过程优化。

### 第三轮：测试与加固

1. T9-01 场景检测测试。
2. T9-02 scene expand 测试。
3. T9-03 端口校验测试。
4. T9-04 参考视频到多镜头验收。
5. T9-05 图像生成与对比验收。
6. T9-06 口播提取到字幕验收。
7. T9-07 文档更新。

## 15. 最小可交付版本

如果需要先做一个最小可用版本，建议只包含以下任务：

```text
T1-01 视频场景检测服务
T1-04 视频反推编排改造
T1-05 工作流执行器支持增强反推
T1-06 前端展示关键帧和 scenes
T2-01 场景展开服务
T2-03 场景展开 API
T2-05 前端展开按钮
T3-02 TextToImage 计费
T4-01 ImageCompare 基础类型
T4-02 ImageCompare 视图
T4-04 ImageCompare 执行器
T8-01 增强 seedance-tvc Skill
```

该版本可达成：

```text
参考视频 → 智能反推 → scenes → 一键搭多镜头画布 → 生成首帧图 → 图像对比 → 图生视频
```

## 16. 任务完成定义

每个任务完成时至少满足：

1. 代码已实现。
2. 前后端类型一致。
3. 节点契约已更新。
4. Agent 工具如涉及已注册。
5. 手动画布路径可用。
6. Agent 路径可用，若任务涉及 Agent。
7. 收费任务已接入确认卡和退款。
8. 失败时有可理解错误。
9. 无新增 linter / type errors。
10. 已补充必要测试或手工验收记录。

## 17. 风险任务标记

高风险任务：

- T1-01 视频场景检测服务：算法效果不稳定。
- T1-03 多模态视觉 LLM 服务：不同 provider 消息格式差异大。
- T3-02 TextToImage 计费：需要避免重复扣费和漏退款。
- T2-02 端口契约校验：需要前后端契约一致。
- T5-01 asset_versions 模型：涉及数据库迁移。
- T7-03 ImageToVideo 首尾帧：provider 能力差异大。

建议这些任务开发时优先写小范围测试或手工验证脚本。

## 18. 最终验收场景

最终整体功能完成后，应能完成以下端到端场景：

```text
1. 用户创建项目。
2. 用户上传产品图。
3. 用户上传参考视频。
4. 用户选择 seedance-tvc Skill。
5. Agent 创建 VideoReversePrompt。
6. Agent 运行视频反推，得到 scenes。
7. Agent 调用 expand_scenes_to_nodes。
8. 系统生成多镜头工作流。
9. 用户确认图像生成扣费。
10. 系统生成首帧图。
11. 用户使用 ImageCompare 选择最佳图。
12. 用户确认视频生成扣费。
13. 系统生成视频镜头。
14. 系统拼接视频。
15. 可选提取或生成口播。
16. 系统烧录字幕。
17. 输出成片。
18. 所有图片、视频、音频、字幕进入素材历史。
```

通过该场景，即说明 SeeMeTVC 已完成对 Tapnow Studio 关键能力的补齐，并形成自身的 Agent 驱动 TVC 生产闭环。
