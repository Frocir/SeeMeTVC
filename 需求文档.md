# SeeMeTVC 对标 Tapnow Studio 补全需求文档

## 1. 文档目的

本文档用于规划 SeeMeTVC 后续功能建设，目标是补全当前项目与 [Tapnow Studio](https://github.com/OrangeBorning/tapnow-studio--) 之间的关键能力差距，并形成更适合 SeeMeTVC 自身定位的产品路线。

SeeMeTVC 不应简单复制 Tapnow Studio 的单文件浏览器工具形态，而应吸收其在可视化节点、多模态创作、视频反推、图像生成、素材管理等方面的能力，最终形成一个更垂直、更平台化的“AI 美妆广告导演工作台”。

## 2. 产品定位

### 2.1 当前项目定位

SeeMeTVC 当前定位为：

> 美妆短片工作台：由 TVC Agent 驱动画布节点出片，支持 Agent + Skill + 对话记忆 + 工作流执行器。

现有核心基础：

- React 工作流画布。
- FastAPI 后端。
- 登录注册与超管后台。
- 视频 / LLM / TTS / 图像渠道配置。
- 余额流水与失败退款。
- 项目素材库。
- TVC Agent。
- `SKILL.md` 导演规程。
- 进程内 MCP 工具。
- 工作流节点执行器。
- Seedance / Agnes / mock 图生视频。
- TTS、裁切、拼接、拆音轨、混音、字幕。

### 2.2 对标对象定位

Tapnow Studio 是一个运行在浏览器中的可视化 AI 工作流工具，采用类似 ComfyUI 的节点编辑方式，将多模型、多模态 API 调用、视频分析、提示词反推、图像对比和素材管理聚合在单文件 HTML 应用中。

Tapnow Studio 的主要优势：

- 轻量，单文件即可运行。
- 无限画布和节点系统。
- 视频智能抽帧和反推。
- 多模型图像生成。
- AI 视频生成任务轮询。
- 图像对比工具。
- 批量素材与历史管理。

### 2.3 SeeMeTVC 的目标差异化

SeeMeTVC 的最终目标不是成为泛 AI 创作工具箱，而是成为：

> AI 美妆广告导演工作台：用户输入产品、卖点、参考图或参考视频，Agent 自动拆 Brief、写分镜、搭画布、生成首帧、调用视频模型、合成口播字幕，并输出可编辑短片。

目标链路：

```text
产品图 / 参考视频 / 卖点 Brief
→ TVC Agent 追问和整理需求
→ 视频反推 / 分镜拆解
→ 首帧图生成
→ Seedance / 视频模型生成镜头
→ TTS / BGM / 字幕
→ 多镜拼接
→ 成片输出
→ 素材和版本沉淀
```

## 3. 当前已补能力

截至本文档编写时，项目已新增以下对标基础能力：

### 3.1 Seedance TVC Skill

新增 `backend/app/skills/seedance-tvc/SKILL.md`，用于指导 Agent：

- 问清品牌、产品、卖点、画幅、时长、禁忌。
- 拆分 1–4 个 Clip。
- 按 Seedance 风格写中文视频 prompt。
- 使用 `TextToImage`、`ImageToVideo`、`TtsSpeak`、`VideoMux`、`SubtitleBurn` 等节点搭图。
- 处理参考图和参考视频。
- 避免视频模型直接生成字幕、水印、法务文案。

### 3.2 OpenAI-compatible 图像渠道

`TextToImage` 已具备真实图像生成入口：

- 支持 `provider=openai`。
- 支持 `provider=openai-compatible`。
- 支持 `/v1/images/generations`。
- 支持本地参考图走 `/v1/images/edits`。
- 支持 `url` 和 `b64_json` 返回。
- 生成结果保存到 `/uploads/{user_id}/...`。

### 3.3 参考视频反推节点

新增 `VideoReversePrompt` 节点能力：

```text
VideoAsset
→ VideoReversePrompt
→ prompt / scenes / frames
→ LlmText 或 ImageToVideo
```

当前能力：

- 上传参考视频。
- ffmpeg 抽取关键帧。
- 调用 LLM 生成分析文本、Seedance prompt 和 scenes。
- 本地模拟 LLM 下有 fallback 输出。
- Agent 可通过 `run_video_reverse_prompt` 调用。

## 4. 与 Tapnow Studio 的主要差距

### 4.1 视频反推深度不足

Tapnow Studio 强调深度视频分析，包括：

- 基于像素差值的智能抽帧。
- 自动识别镜头切换。
- 导演级分镜拆解。
- 运镜手法分析。
- 主体动态分析。
- 光影氛围分析。
- 提示词反推。
- 口播提取。

SeeMeTVC 当前仍停留在：

```text
固定抽帧
→ LLM 文本反推
```

不足：

- 无真实场景检测。
- 无镜头切换点识别。
- 无镜头起止时间轴。
- 无 ASR 口播提取。
- 无多模态视觉模型真实读帧。
- 无 Midjourney 英文 prompt 与即梦中文 prompt 双版本输出。

### 4.2 多模态视觉理解不足

当前 `VideoReversePrompt` 主要把关键帧 URL 交给 LLM。若 LLM 渠道不是视觉模型，则模型无法真正理解画面内容。

需要补：

- 关键帧转 base64。
- 支持 OpenAI / Anthropic / Gemini 风格多模态消息。
- 识别主体、构图、动作、材质、色彩、光线。
- 每帧生成结构化分析。

### 4.3 图像生成能力不完整

当前项目已接入 OpenAI-compatible 图像渠道，但还缺少 Tapnow Studio 式的完整图像工作流。

不足：

- 缺少图像生成计费。
- 缺少图像生成确认卡。
- 缺少分辨率选择。
- 缺少图生图强度参数。
- 缺少多图参考。
- 缺少角色一致性参考。
- 缺少风格一致性参考。
- 缺少局部重绘。
- 缺少 negative prompt。
- 缺少 seed。
- 缺少批量生成。
- 缺少即梦图生图 / Midjourney / Flux / Nano Banana 等多模型适配。

### 4.4 缺少图像对比节点

Tapnow Studio 提供 Image Compare，用于 AB 图像对比。

SeeMeTVC 当前缺少：

```text
ImageCompare
```

美妆场景中该功能非常重要，可用于：

- 产品原图 vs AI 首帧图。
- 修图前 vs 修图后。
- 素颜图 vs 妆容图。
- A 风格 vs B 风格。
- 不同模型生成结果对比。

### 4.5 素材历史和批量管理不足

Tapnow Studio 有历史记录管理器和批量素材操作。

SeeMeTVC 当前已有素材库，但仍缺：

- 节点级生成历史。
- 每次生成版本沉淀。
- 批量删除。
- 批量重新发送到画布。
- 收藏最佳结果。
- 从历史结果继续生成。
- 按模型 / 日期 / 节点筛选。
- 失败结果清理。

### 4.6 画布交互成熟度不足

Tapnow Studio 强调无限画布、多选、批量操作、实时预览和进度条。

SeeMeTVC 当前需要增强：

- 框选多节点。
- 批量移动。
- 批量删除。
- 节点复制粘贴。
- 自动布局。
- 一键整理画布。
- 节点分组。
- 节点折叠。
- 运行进度条。
- 失败节点一键重试。
- Agent 自动排布节点更规整。

### 4.7 多视频模型不足

Tapnow Studio 提到 Sora、Grok Video、Veo 等视频模型。

SeeMeTVC 当前主要支持：

- Seedance / Ark。
- Agnes / Pavo。
- mock。

后续需要逐步扩展：

- Veo。
- Kling。
- Wan。
- Sora。
- LibTV。
- Runway。
- Luma。
- Pika。
- 即梦视频。

但该项不应优先于垂直出片闭环。

### 4.8 缺少首帧 / 尾帧控制

当前已有：

```text
TextToImage → ImageToVideo
```

但还缺：

- 首帧图。
- 尾帧图。
- 首尾帧视频。
- 风格参考图。
- 角色参考图。
- 产品参考图。
- 多参考图权重。

### 4.9 缺少 ASR 口播提取

Tapnow Studio 提到口播提取能力。

SeeMeTVC 当前有：

- 拆音轨。
- TTS。
- 音频裁切。
- 混音。
- 字幕烧录。

但缺少：

```text
视频 / 音频 → ASR → 时间轴脚本 / 字幕 / narration
```

### 4.10 缺少 scenes 自动展开能力

当前 `VideoReversePrompt` 可以输出 scenes，但还不能稳定自动展开为多镜头画布。

需要补：

```text
scenes
→ 多个 ImageToVideo 节点
→ VideoMux
→ 可选 TTS / MixAudio / SubtitleBurn
```

可以通过 Agent 工具或专门后端工具实现。

### 4.11 快速配置体验不足

Tapnow Studio 是单文件工具，用户可直接打开并配置 API Key。

SeeMeTVC 因为平台化架构，需要：

- 后端。
- 登录。
- 超管。
- 渠道配置。
- Key 管理。

这更适合 SaaS，但对个人创作者试用门槛更高。

需要补：

- 首次启动向导。
- 渠道检测。
- 一键测试 LLM / 图像 / 视频 / TTS。
- 示例项目自动创建。
- 本地模拟演示路线。

## 5. 总体建设目标

### 5.1 近期目标

把 SeeMeTVC 做成一个能稳定完成以下任务的工作台：

```text
用户上传产品图或参考视频
→ 选择 seedance-tvc Skill
→ Agent 自动追问缺失信息
→ 生成分镜和 Seedance prompt
→ 生成首帧图
→ 生成视频镜头
→ 拼接成片
→ 添加口播、字幕、BGM
→ 保存素材历史
```

### 5.2 中期目标

补齐 Tapnow Studio 的关键创作者工具：

- 智能场景检测。
- 多模态关键帧分析。
- 图像对比节点。
- 素材历史管理。
- 图像生成参数面板。
- 首尾帧控制。
- ASR 口播提取。

### 5.3 长期目标

形成区别于 Tapnow Studio 的垂直平台能力：

- 多用户。
- 多项目。
- 团队协作。
- 渠道成本管理。
- 余额 / 套餐 / 计费。
- 品牌资产库。
- 人物 / 产品 / 风格库。
- Skill 市场。
- 外部 MCP 接入。
- 批量广告生产。

## 6. 需求优先级

## P0：必须优先补齐

### P0-1 智能场景检测与关键帧提取

#### 背景

当前 `VideoReversePrompt` 是固定抽帧，不具备真正镜头切分能力。

#### 目标

实现类似 Tapnow Studio `detectScenesAndCapture` 的能力：

```text
输入视频
→ 采样帧
→ 计算相邻帧差异
→ 识别镜头切换点
→ 输出关键帧与时间轴
```

#### 功能要求

- 支持 mp4 / webm / mov。
- 支持配置最大关键帧数量。
- 支持配置帧差阈值。
- 输出每个镜头：
  - `start_time`
  - `end_time`
  - `frame_url`
  - `score`
- 若检测不到切换点，回退为等间隔抽帧。
- 抽帧图片保存到项目上传目录。

#### 验收标准

- 上传 10–30 秒参考视频后，能自动提取 3–8 张关键帧。
- 镜头切换明显的视频，关键帧能落在不同镜头。
- 结果可在 `VideoReversePrompt` 节点中查看。

### P0-2 多模态关键帧分析

#### 背景

当前 LLM 可能无法真正读取关键帧图像。

#### 目标

接入支持图像输入的 LLM，让模型真正分析关键帧内容。

#### 功能要求

- 支持 OpenAI-compatible vision。
- 支持 Anthropic vision。
- 预留 Gemini vision。
- 本地上传图片可转为 base64 或可访问 URL。
- 每帧输出：
  - 主体。
  - 场景。
  - 景别。
  - 运镜。
  - 光线。
  - 色彩。
  - 材质。
  - 动作。
  - 可复用 Seedance prompt。

#### 验收标准

- 给定产品参考视频，模型能描述产品颜色、材质、环境和镜头语言。
- 输出不是泛泛模板，而是与关键帧内容相关。

### P0-3 scenes 自动展开为多镜头工作流

#### 背景

当前反推结果中的 scenes 不能稳定自动变成画布节点。

#### 目标

新增工具或按钮，将 scenes 自动展开为可执行节点链。

#### 推荐方案

新增 Agent 工具：

```text
expand_scenes_to_nodes
```

或新增前端按钮：

```text
从分镜生成工作流
```

#### 功能要求

输入：

```json
{
  "source_node_id": "VideoReversePrompt 节点 ID",
  "mode": "silent | with_tts | with_image"
}
```

输出画布结构：

```text
每个 scene → LlmText 或 TextAsset
每个 scene → 可选 TextToImage
每个 scene → ImageToVideo
多个 ImageToVideo → VideoMux
可选 TTS → MixAudio
可选 SubtitleBurn
```

#### 验收标准

- 反推出 3 个 scenes 后，一键生成 3 条视频镜头节点。
- 自动连线正确。
- 一键跑能通过预检。

### P0-4 图像生成计费与确认卡

#### 背景

真实图像生成已接入，但还没有和视频一样的成本确认链路。

#### 目标

让 `TextToImage` 也纳入余额和确认体系。

#### 功能要求

- `Channel.kind=image` 支持单次成本字段。
- `TextToImage` 运行前估算成本。
- 成本大于 0 时显示确认卡。
- 成功扣费。
- 失败退款。
- 本地模拟不扣费。

#### 验收标准

- 启用收费图像模型时，Agent 运行 `run_text_to_image` 会弹确认卡。
- 用户取消不扣费。
- 上游失败自动退款。

### P0-5 ImageCompare 图像对比节点

#### 背景

美妆广告强依赖前后效果对比。

#### 目标

新增图像对比节点，支持 AB 对比。

#### 节点定义

```text
ImageCompare
输入：before image, after image
输出：selected image，可选
```

#### 功能要求

- 支持左右并排。
- 支持滑杆对比。
- 支持全屏查看。
- 支持交换 before / after。
- 支持选择 A 或 B 继续下游生成。

#### 验收标准

- 两张图片接入后，节点内可滑动对比。
- 双击可全屏查看。
- 可将选中结果传给后续 `ImageToVideo`。

## P1：重要体验增强

### P1-1 素材历史与版本管理

#### 目标

把素材库升级为创作历史中心。

#### 功能要求

- 每次节点生成都保存历史版本。
- 历史记录关联：
  - 项目 ID。
  - 节点 ID。
  - 模型 ID。
  - prompt。
  - 生成时间。
  - 成本。
  - 状态。
- 支持收藏。
- 支持删除。
- 支持批量删除。
- 支持重新发送到画布。
- 支持从该结果继续生成。

#### 验收标准

- 用户能查看某个项目内所有生成图 / 视频 / 音频。
- 用户能把历史结果重新变成 `ImageAsset` / `VideoAsset`。

### P1-2 画布多选与批量操作

#### 目标

提升节点编辑器成熟度。

#### 功能要求

- 框选多节点。
- 批量移动。
- 批量删除。
- 批量复制粘贴。
- 一键整理选中节点。
- 自动布局当前链路。

#### 验收标准

- 用户可框选一条多镜链路并整体移动。
- 删除节点时自动删除相关边。

### P1-3 首帧 / 尾帧控制

#### 目标

增强视频生成可控性。

#### 方案

扩展 `ImageToVideo` 或新增节点：

```text
FirstLastFrameVideo
```

#### 功能要求

- 支持首帧图。
- 支持尾帧图。
- 支持风格参考图。
- 支持产品参考图。
- 支持角色参考图。
- 支持不同模型能力降级。

#### 验收标准

- 支持首尾帧模型时，提交首尾帧。
- 不支持首尾帧模型时，前端给出提示，不允许错误提交。

### P1-4 ASR 口播提取

#### 目标

从参考视频中提取口播和时间轴脚本。

#### 新增节点

```text
SpeechToText 或 VideoTranscribe
```

#### 功能要求

- 输入视频或音频。
- 输出 transcript。
- 输出 segments。
- 输出 srt。
- 可接到 `SubtitleBurn`。
- 可接到 `LlmText` 用于改写口播。

#### 验收标准

- 上传有口播的视频，能得到可编辑文案。
- 能生成字幕文件或字幕文本。

### P1-5 图像参数面板

#### 目标

增强图像生成控制。

#### 功能要求

- 分辨率。
- 画幅。
- seed。
- negative prompt。
- 参考图权重。
- 风格强度。
- batch size。
- 图生图强度。
- 模型能力差异提示。

#### 验收标准

- `TextToImage` 检查器根据当前模型展示可用参数。
- 不支持的参数不显示或置灰。

## P2：生态扩展

### P2-1 多图像模型适配

候选模型：

- 即梦图像。
- Midjourney。
- Flux。
- DALL-E。
- Nano Banana。
- Seedream。
- LibTV image。

要求：

- 统一 Channel 配置。
- 保留 provider-specific 参数。
- 支持模型能力矩阵。

### P2-2 多视频模型适配

候选模型：

- Veo。
- Kling。
- Wan。
- Sora。
- LibTV。
- Runway。
- Luma。
- Pika。
- 即梦视频。

要求：

- 支持文生视频。
- 支持图生视频。
- 支持首尾帧视频。
- 支持时长限制。
- 支持画幅限制。
- 支持是否原生音频标记。

### P2-3 Skill 市场

目标：

- 不同导演风格可作为 Skill 安装。
- 支持项目级 Skill。
- 支持全局 Skill。
- 支持启用 / 禁用。
- 支持版本管理。

### P2-4 外部 MCP 接入

目标：

- 将当前进程内 MCP 暴露为受控 JSON-RPC / HTTP MCP 服务。
- 允许 Cursor / Claude / 其他 Agent 连接。

要求：

- 鉴权。
- 只读 / 写图权限隔离。
- 项目级访问控制。
- 工具调用审计。

### P2-5 首次配置向导

目标：

降低个人设计师试用门槛。

功能：

- 检查 LLM / 图像 / 视频 / TTS 渠道状态。
- 引导填写 Key。
- 一键测试渠道。
- 创建示例项目。
- 展示 mock 模式能力。

## 7. 推荐实施路线

### 阶段一：把参考视频反推做实

目标：从“固定抽帧模板”升级为“智能视频分析”。

任务：

1. 智能场景检测。
2. 多模态关键帧分析。
3. 逐镜头 scenes 输出。
4. scenes 一键展开成多镜头画布。

交付结果：

```text
上传参考视频
→ 自动拆分镜
→ 自动生成 Seedance prompt
→ 自动搭多镜头画布
```

### 阶段二：补强图像生成闭环

任务：

1. 图像生成计费和确认卡。
2. 图像参数面板。
3. 图生图强度和分辨率。
4. ImageCompare 节点。
5. 图像生成历史。

交付结果：

```text
Brief
→ 首帧图生成
→ AB 对比
→ 选择最佳图
→ 图生视频
```

### 阶段三：补强素材历史与创作效率

任务：

1. 素材历史中心。
2. 批量删除。
3. 批量发送到画布。
4. 从历史继续生成。
5. 画布多选与自动布局。

交付结果：

```text
用户可以围绕一个广告项目反复试图、试视频、回滚和复用版本。
```

### 阶段四：补强声音和字幕链路

任务：

1. ASR 口播提取。
2. 时间轴字幕。
3. 字幕样式编辑。
4. 口播改写。
5. TTS 重配音。

交付结果：

```text
参考视频口播
→ 自动提取
→ 改写成品牌口播
→ TTS
→ 字幕烧录
```

### 阶段五：扩展模型生态和平台能力

任务：

1. 即梦 / Midjourney / Flux / Nano Banana。
2. Veo / Kling / Wan / LibTV。
3. Skill 市场。
4. 外部 MCP。
5. 首次配置向导。

交付结果：

```text
SeeMeTVC 成为可商业化、多渠道、多风格、多用户的 AI 美妆广告工作台。
```

## 8. 关键节点新增规划

### 8.1 VideoReversePrompt 增强版

现有节点继续保留，但增强字段：

```text
scene_threshold
max_scenes
frame_strategy: fixed | scene_detect
vision_model_id
prompt_style: seedance | midjourney | jimeng
```

输出：

```text
prompt
text
scenes
frames
timeline
midjourney_prompt
jimeng_prompt
transcript
```

### 8.2 ImageCompare

输入：

```text
before: image
after: image
```

输出：

```text
image: selected image
```

字段：

```text
mode: slider | side_by_side
selected: before | after
```

### 8.3 SpeechToText

输入：

```text
audio 或 video
```

输出：

```text
text
segments
srt
```

### 8.4 SceneExpand

输入：

```text
scenes
```

输出或行为：

```text
自动创建多镜头节点链
```

可作为 Agent 工具优先实现，不一定需要前端可视节点。

### 8.5 FirstLastFrameVideo

输入：

```text
prompt
first_image
last_image
style_image
character_image
```

输出：

```text
video
```

## 9. 数据结构建议

### 9.1 Scene 数据结构

```json
{
  "id": "scene_001",
  "title": "Clip 01",
  "start_time": 0,
  "end_time": 4.2,
  "frame_url": "/uploads/1/xxx_frame1.jpg",
  "analysis": "产品置于柔光梳妆台中央，镜头缓慢推进。",
  "prompt": "参考产品图，保持瓶身形状、logo、颜色不变。镜头从中景缓慢推进到标签特写...",
  "narration": "让肌肤在光里自然透亮。",
  "negative_prompt": "不要字幕、水印、错字、额外 logo"
}
```

### 9.2 Asset Version 数据结构

```json
{
  "workflow_id": 1,
  "node_id": "t2i_001",
  "kind": "image",
  "url": "/uploads/1/xxx.png",
  "model_id": "gpt-image-1",
  "prompt": "...",
  "cost": 1.2,
  "status": "succeeded",
  "favorite": false,
  "created_at": "..."
}
```

### 9.3 Model Capability 数据结构

```json
{
  "model_id": "seedance-2.5",
  "kind": "video",
  "supports_text_to_video": true,
  "supports_image_to_video": true,
  "supports_first_last_frame": false,
  "supports_audio": true,
  "duration_min": 4,
  "duration_max": 30,
  "aspects": ["16:9", "9:16", "1:1"]
}
```

## 10. 非目标

以下方向暂不作为近期目标：

- 不把 SeeMeTVC 改成单 HTML 应用。
- 不优先复制 Tapnow Studio 的所有泛模型入口。
- 不优先做无后端的本地工具形态。
- 不优先做 Skill 商店，先让内置 Skill 跑顺。
- 不优先外开 MCP，先确保内部 Agent 可靠。

## 11. 成功指标

### 11.1 创作闭环指标

- 用户上传一个产品图和一句卖点，10 分钟内能得到一个可播放短片。
- 用户上传一个参考视频，系统能自动拆出 2–5 个镜头建议。
- Agent 能自动搭建可一键运行的多镜头画布。

### 11.2 质量指标

- 反推 prompt 与参考视频画面明显相关。
- 首帧图能保持产品形态和品牌元素。
- 生成视频无明显错字、水印、无关字幕。
- 失败时有明确错误和可重试路径。

### 11.3 效率指标

- 素材历史可复用，不需要反复上传。
- 多镜头工作流可自动展开。
- 生成结果可对比、收藏、继续生成。

### 11.4 平台指标

- 图像、视频、TTS 都纳入统一渠道管理。
- 收费模型都支持确认卡和失败退款。
- 项目资产可追踪来源节点和模型。

## 12. 最终目标

SeeMeTVC 最终应补齐与 Tapnow Studio 的关键差距，并形成自己的优势：

```text
Tapnow Studio：轻量化、多模型、可视化 AI 创作工具箱。
SeeMeTVC：平台化、Agent 驱动、垂直美妆 TVC 自动出片工作台。
```

最终用户体验目标：

```text
用户：上传产品图和参考视频，说“帮我做一条 15 秒小红书美妆广告”。

系统：
1. 自动识别参考视频镜头。
2. 自动提取关键帧和口播。
3. 自动生成分镜脚本和 Seedance prompt。
4. 自动生成首帧图。
5. 自动搭建多镜头视频节点。
6. 用户确认扣费。
7. 自动生成视频片段。
8. 自动拼接、配音、字幕。
9. 输出成片，并保留所有素材版本。
```

这就是 SeeMeTVC 对标 Tapnow Studio 后的补全方向，也是项目后续从“工作流雏形”走向“AI 广告生产系统”的核心路线。
