# SeeMeTVC 新手教程

美妆 / 硬件短片工作台。先跟左边的 **TVC Agent** 把方案说清楚，中间画布上会出现可改、可再跑的工作流节点。会扣费的步骤会在对话里先问你，确认前不生成、不扣费。

下面按「登录 → 进项目 → 认三栏 → 出一条片」走。

---

## 30 秒看懂

先登录，进入主页。左侧 **工作区** → 打开已有项目（最好选一条已经跑过 Clip 的成片），或 **模板** 里切「美学」/「硬件 / 科创」，点一张 Lookbook 图新建。进入无限画布后有三个区域：

| 区域 | 是什么 |
| --- | --- |
| **左：TVC Agent** | 默认 Plan。先出方案卡（Brief → 分镜 → 搭图），你点「开始」再动手。文生图 / 图生视频会弹出对话内确认提问。Skill 默认 `seedance-tvc`。 |
| **中：无限画布** | 真工作流节点：文本、LLM 文案、文生图、图生视频、反推参考片、TTS、ASR、裁切、拼接、混音、字幕。可手搭，也可让 Agent 搭完后点顶栏「一键排版」。 |
| **右：检查器 / 素材** | 点中节点后改提示词、参考图（上传自动压缩）、模型（Lite / 2.5 参考图槽位不同）。生成历史可丢回画布。 |

画布顶栏还有「新手教程演示」，是同一套说明的现场版。

---

## 1. 登录，进入主页

1. 打开站点（本机开发默认 `http://127.0.0.1:5173`）。
2. 用已有账号登录，或点「没有账号？注册」。注册会送一笔试用积分。
3. 点「开始创作」。左侧栏默认停在 **工作区**，这就是主页。

开发态登录页可能已预填超管邮箱，可直接进。不必找「超管」菜单——演示里它是藏着的。

---

## 2. 打开一个项目

两条路，选一条：

**A. 工作区（推荐评委 / 复用成片）**

- 点左侧 **工作区**。
- 打开一条已经跑过 Clip 的项目（封面往往能播）。适合先看成品，再让 Agent「再跑一遍」。

**B. 模板（从零）**

- 点左侧 **模板**。
- 上面是流程卡：美学（成片 / 快测）、硬件 / 科创（成片 / 工坊 / 快测）。
- 下面是 **Lookbook**：切「美学」或「硬件 / 科创」，点一张图 → 自动新建项目，并把画面描述写进文案。

进入后地址类似 `/workflow/…`，这就是无限画布。左上角「← 工作区」可返回。

---

## 3. 认三栏

### 左：TVC Agent

默认停在 **TVC Agent** 页签（旁边还有「工具」「素材」）。

- 模式选 **Plan**（默认）：先出方案卡，Brief → 分镜 → 搭图，每一环要你点「开始」或「批准计划」。
- **Auto**：四件套齐了就直接搭，但扣费确认提问两种模式都不跳。
- Skill 选 **seedance-tvc**（美妆 / 硬件短片导演）。空着则没有导演规程。
- 对话模型默认 DeepSeek-V4-Pro（列表里有已启用的渠道时可换）。
- 输入框示例：「跟我说品牌和想要的感觉，我先帮你把方案想清楚」。
- 右上角 **×** 清空对话，画布不动。

出片前会出现对话内卡片，例如「确认开始」，并写预估积分。点确认才扣费。

### 中：无限画布

- 拖动画布、滚轮缩放；从节点圆点连到下一个节点。
- 常见成片链：文生图 → ImageToVideo → VideoMux（拼接）。
- Agent 搭完若叠在一起，点顶栏 **一键排版**。
- **开始生成** 会按连线整条跑（和 Agent 的 `run_*` 是同一套执行器）。只要跑当前节点，点中后用检查器里的「生成这一步」。

### 右：检查器 / 素材

- 点中一个节点，右边改提示词、时长、参考图。大图上传会自动压缩。
- Seedance **Lite** 与 **2.5** 参考图槽位不同（2.5 才有风格 / 角色 / 产品参考图等）。
- 左侧切到 **素材** 页签，可把生成历史拖回画布。

---

## 4. 第一次跟 Agent 出片

确认左侧是 **TVC Agent**、模式 **Plan**、Skill **seedance-tvc**。在输入框粘贴：

```text
跑 Clip3 文生图，再 ImageToVideo，最后 VideoMux 拼成完整片子。
```

然后按这个顺序点：

1. 出现 **片子方案** 卡 → 点「批准计划」。
2. 出现某一环（Brief / 分镜 / 搭图）→ 点「开始」。
3. 画布上出现或更新节点；Agent 要跑文生图 / 图生视频时弹出 **确认开始** → 看预估积分，再确认。
4. 左侧会出现工具进度（文生图、图生视频、拼接）。上游要几十秒到几分钟，不是页面卡死。
5. 成片在 **VideoMux**（拼接）节点上，点开可播。

再改同一镜，直接说：

```text
再跑一遍出视频
```

不必重搭图。弹出确认提问时，先确认或取消，不要连发多条「再跑一遍」。

---

## 5. 可选：自己搭，不靠 Agent

左侧切 **工具**，按「素材 → 生成 → 剪辑」往画布加点，连好线，改右边参数，再点顶栏 **开始生成**。第一次进画布可用顶栏 **新手教程演示**。

---

## 不要这样

- 两个浏览器标签同时改同一个项目（后写覆盖）。
- 确认提问还在时又发一条新指令。
- 把人物库当成坏了——那是空页，图请在项目里上传。
- 没有视频 / LLM 渠道 Key 时，方案卡能出，片出不来。请超管填 Key（头像 2 秒内连点 5 次打开超管页）。
