# SeeMeTVC 对标 Tapnow Studio 补全系统设计文档

## 1. 文档定位

本文档基于 `需求文档.md` 与 `方案文档.md`，进一步细化为系统设计说明，用于指导后续工程实现。

本文档重点回答：

- 系统应拆成哪些模块。
- 每个模块的职责边界是什么。
- 节点、数据结构、API、服务接口如何设计。
- 前后端如何协作。
- Agent 如何复用这些能力。
- 如何保证计费、确认卡、素材历史、模型能力矩阵的一致性。

本文档不是需求清单，也不是排期计划，而是面向开发实现的设计蓝图。

## 2. 设计目标

### 2.1 产品目标

将 SeeMeTVC 从当前的“Agent + 节点画布出片雏形”升级为：

> Agent 驱动的 AI 美妆 TVC 生产系统。

核心用户路径：

```text
上传产品图 / 参考视频 / 输入 Brief
→ TVC Agent 追问并整理需求
→ 视频反推拆分镜
→ 生成首帧图
→ 图像对比选择
→ 图生视频
→ 口播 / 字幕 / BGM
→ 拼接成片
→ 素材历史沉淀
```

### 2.2 技术目标

1. 不重写工作流引擎，继续复用 `workflow_exec.py`。
2. 所有新增能力均以节点、服务、MCP 工具三层复用。
3. Agent 自动搭图与用户手动画布共用同一套节点契约。
4. 所有模型调用都走统一 Channel。
5. 所有收费模型调用都支持确认、扣费、失败退款。
6. 所有生成结果都能沉淀为素材历史。
7. 模型差异通过 capabilities 描述，前端不硬编码 provider 规则。

## 3. 总体架构设计

### 3.1 分层架构

```text
┌──────────────────────────────────────────────┐
│ Frontend                                     │
│ React Workflow Canvas                         │
│ - 节点渲染                                    │
│ - 检查器                                      │
│ - 运行确认卡                                  │
│ - 素材历史                                    │
└───────────────────▲──────────────────────────┘
                    │ REST / SSE
┌───────────────────┴──────────────────────────┐
│ Backend API                                   │
│ - 工作流运行 API                              │
│ - 场景展开 API                                │
│ - 素材历史 API                                │
│ - Channel capability API                      │
└───────────────────▲──────────────────────────┘
                    │
┌───────────────────┴──────────────────────────┐
│ Workflow Runtime                              │
│ - workflow_exec.py                            │
│ - 节点执行                                    │
│ - 端口校验                                    │
│ - 确认卡 / 扣费 / 退款                         │
└───────────────────▲──────────────────────────┘
                    │
┌───────────────────┴──────────────────────────┐
│ Domain Services                               │
│ - video_reverse                               │
│ - video_scene_detect                          │
│ - vision_llm                                  │
│ - scene_expand                                │
│ - image_gen                                   │
│ - asr                                         │
│ - asset_versions                              │
│ - model_capabilities                          │
└───────────────────▲──────────────────────────┘
                    │
┌───────────────────┴──────────────────────────┐
│ Providers / External Tools                    │
│ - LLM / Vision LLM                            │
│ - OpenAI-compatible Image                     │
│ - Seedance / Agnes / Video Providers          │
│ - ASR Providers                               │
│ - ffmpeg                                      │
└──────────────────────────────────────────────┘
```

### 3.2 Agent 调用路径

```text
User Chat
→ agent_runtime.py
→ skill_loader.py 加载 seedance-tvc
→ mcp_server.py 工具调用
→ graph_ops.py 修改画布
→ workflow_exec.py 执行节点
→ domain services 调模型 / ffmpeg
→ SSE 返回过程与结果
```

Agent 不直接调用外部模型；Agent 只能通过节点运行工具或图工具间接完成工作。

### 3.3 手动画布调用路径

```text
用户手动拖节点 / 连线 / 改参数
→ 保存 graph_json
→ 点击运行节点或全图运行
→ workflow_exec.py
→ domain services
→ 写回节点 data
→ 前端刷新结果
```

Agent 和手动画布共享 `workflow_exec.py`，避免出现两套出片逻辑。

## 4. 核心领域对象设计

### 4.1 Workflow Graph

当前项目已经以 `graph_json` 保存画布。后续继续沿用。

建议标准化 graph 节点结构：

```json
{
  "id": "node_001",
  "type": "VideoReversePrompt",
  "position": { "x": 100, "y": 100 },
  "data": {
    "label": "参考视频反推",
    "kind": "prompt",
    "video_url": "/uploads/1/ref.mp4",
    "frame_strategy": "scene_detect",
    "max_scenes": 6,
    "scene_threshold": 0.28,
    "prompt_style": "seedance"
  }
}
```

边结构：

```json
{
  "id": "edge_001",
  "source": "node_001",
  "sourceHandle": "scenes",
  "target": "node_002",
  "targetHandle": "prompt"
}
```

### 4.2 Node Data

节点 `data` 应同时承担三类信息：

1. 用户输入参数。
2. 执行后的输出结果。
3. UI 展示状态。

建议约定：

```json
{
  "label": "节点标题",
  "kind": "image | video | audio | text | prompt | scenes",
  "status": "idle | running | succeeded | failed | pending_confirm",
  "error": null,
  "params": {},
  "outputs": {},
  "ui": {}
}
```

由于当前项目已直接在 `data` 顶层放字段，短期不强制迁移到 `params/outputs/ui`，但新增字段命名应尽量稳定，避免重复含义。

### 4.3 Scene

`Scene` 是视频反推和多镜头展开的核心结构。

```typescript
type Scene = {
  id: string;
  index: number;
  title: string;
  start_time?: number;
  end_time?: number;
  frame_url?: string;
  score?: number;
  analysis: string;
  prompt: string;
  seedance_prompt?: string;
  midjourney_prompt?: string;
  jimeng_prompt?: string;
  narration?: string;
  negative_prompt?: string;
};
```

后端 Pydantic 对应：

```python
class SceneOut(BaseModel):
    id: str
    index: int
    title: str
    start_time: float | None = None
    end_time: float | None = None
    frame_url: str | None = None
    score: float | None = None
    analysis: str
    prompt: str
    seedance_prompt: str | None = None
    midjourney_prompt: str | None = None
    jimeng_prompt: str | None = None
    narration: str | None = None
    negative_prompt: str | None = None
```

### 4.4 Timeline Item

```typescript
type TimelineItem = {
  index: number;
  start_time: number;
  end_time: number;
  frame_url: string;
  score: number;
};
```

### 4.5 Asset Version

`AssetVersion` 用于记录每次节点生成结果。

```typescript
type AssetVersion = {
  id: number;
  user_id: number;
  project_id: number;
  workflow_id?: number;
  node_id: string;
  node_type: string;
  kind: "image" | "video" | "audio" | "text" | "prompt";
  url?: string;
  thumbnail_url?: string;
  text?: string;
  model_provider?: string;
  model_name?: string;
  channel_id?: number;
  prompt?: string;
  params_json?: Record<string, unknown>;
  cost?: number;
  status: "succeeded" | "failed";
  error_message?: string;
  favorite: boolean;
  created_at: string;
  updated_at: string;
};
```

## 5. 节点设计

## 5.1 VideoReversePrompt 增强节点

### 5.1.1 职责

将参考视频转换为结构化创作资产：

```text
video → frames / timeline / scenes / prompt
```

### 5.1.2 输入端口

```text
video: video
brief: text，可选
```

### 5.1.3 输出端口

```text
prompt: prompt
scenes: scenes
frames: image[]
timeline: json
```

### 5.1.4 data 字段

```typescript
type VideoReversePromptData = WfData & {
  video_url?: string;
  reference_video_url?: string;
  frame_strategy?: "fixed" | "scene_detect";
  frame_count?: number;
  max_scenes?: number;
  scene_threshold?: number;
  sample_fps?: number;
  prompt_style?: "seedance" | "midjourney" | "jimeng" | "all";
  vision_channel_id?: number;
  text?: string;
  prompt?: string;
  frames?: string[];
  timeline?: TimelineItem[];
  scenes?: Scene[];
};
```

### 5.1.5 执行流程

```text
_exec_video_reverse
→ 解析上游 video_url / brief
→ video_reverse.reverse_prompt
→ video_scene_detect.detect_scenes
→ vision_llm.analyze_frames
→ assemble scenes
→ 写回 node.data
```

### 5.1.6 失败策略

- 视频不存在：节点失败。
- ffmpeg 不可用：节点失败并给出明确错误。
- 场景检测为空：回退固定抽帧。
- Vision LLM 不可用：回退文本模板分析。
- JSON 解析失败：保留 raw_text，并生成 fallback scenes。

## 5.2 ImageCompare 节点

### 5.2.1 职责

提供图片 AB 对比，并选择其中一张继续下游工作流。

### 5.2.2 输入端口

```text
before: image
after: image
```

### 5.2.3 输出端口

```text
image: image
```

### 5.2.4 data 字段

```typescript
type ImageCompareData = WfData & {
  before_url?: string;
  after_url?: string;
  selected?: "before" | "after";
  compare_mode?: "slider" | "side_by_side";
  url?: string;
};
```

### 5.2.5 执行流程

```text
_exec_image_compare
→ 从上游读取 before_url / after_url
→ 根据 selected 选择输出 url
→ 写入 kind=image
```

该节点不调用外部模型，不扣费。

## 5.3 SpeechToText 节点

### 5.3.1 职责

从音频或视频中提取口播文本、segments 和 SRT。

### 5.3.2 输入端口

```text
media: audio | video
```

### 5.3.3 输出端口

```text
text: text
segments: json
srt: text
```

### 5.3.4 data 字段

```typescript
type SpeechToTextData = WfData & {
  media_url?: string;
  provider?: "local-simulate" | "openai-compatible";
  language?: string;
  text?: string;
  segments?: Array<{
    start: number;
    end: number;
    text: string;
  }>;
  srt?: string;
};
```

### 5.3.5 执行流程

```text
_exec_speech_to_text
→ 如果输入是 video，先 demux 音频
→ asr.transcribe
→ 输出 text / segments / srt
```

## 5.4 ImageToVideo 首尾帧扩展

### 5.4.1 职责

在现有图生视频基础上支持更多参考图输入。

### 5.4.2 新增 data 字段

```typescript
type ImageToVideoData = WfData & {
  first_image_url?: string;
  last_image_url?: string;
  style_image_url?: string;
  character_image_url?: string;
  product_image_url?: string;
  reference_strength?: number;
};
```

### 5.4.3 端口建议

短期：不新增过多端口，先在 Inspector 中选择参考图。

中期：增加端口：

```text
first_image: image
last_image: image
style_image: image
character_image: image
product_image: image
```

### 5.4.4 能力校验

运行前调用 `model_capabilities`：

```text
如果 channel 不支持 first_last_frame，但存在 last_image_url → 阻止运行
如果 channel 不支持 style_reference，但存在 style_image_url → 阻止运行
```

## 6. 后端服务设计

## 6.1 `video_scene_detect.py`

### 6.1.1 职责

从视频中检测镜头边界并提取关键帧。

### 6.1.2 主要接口

```python
async def detect_scenes(
    video_url: str,
    *,
    user_id: int | None,
    max_scenes: int = 8,
    threshold: float = 0.28,
    sample_fps: float = 2.0,
    min_scene_duration: float = 0.8,
) -> list[DetectedScene]:
    ...
```

### 6.1.3 内部步骤

```text
resolve local path
→ ffprobe 获取时长
→ ffmpeg 抽采样帧
→ PIL / OpenCV 计算帧差
→ 根据 threshold 识别边界
→ 合并短镜头
→ 选择中间帧
→ 保存关键帧
→ 返回 DetectedScene[]
```

### 6.1.4 依赖选择

第一版优先使用：

- ffmpeg / ffprobe。
- Pillow。

如果当前 requirements 未包含 Pillow，则可选择：

- 添加 Pillow。
- 或用 ffmpeg `signature` / `select` 过滤器，但可控性较弱。

## 6.2 `vision_llm.py`

### 6.2.1 职责

封装多模态 LLM 调用。

### 6.2.2 主要接口

```python
async def analyze_video_frames(
    frames: list[DetectedScene],
    *,
    brief: str | None,
    style: str,
    channel_id: int | None,
    user_id: int | None,
) -> list[FrameAnalysis]:
    ...
```

### 6.2.3 Channel 选择

优先级：

1. 用户指定 `vision_channel_id`。
2. 启用的 `kind=llm` 且 `config_json.capabilities.vision=true`。
3. 普通 LLM fallback。
4. local-simulate fallback。

### 6.2.4 OpenAI-compatible vision 消息格式

```json
{
  "model": "gpt-4o-mini",
  "messages": [
    {
      "role": "system",
      "content": "你是美妆广告分镜导演..."
    },
    {
      "role": "user",
      "content": [
        { "type": "text", "text": "请分析以下关键帧..." },
        {
          "type": "image_url",
          "image_url": {
            "url": "data:image/jpeg;base64,..."
          }
        }
      ]
    }
  ],
  "response_format": { "type": "json_object" }
}
```

### 6.2.5 输出容错

```text
raw response
→ extract json block
→ json.loads
→ Pydantic validate
→ 缺字段补默认值
→ 完全失败时 fallback
```

## 6.3 `video_reverse.py`

### 6.3.1 职责

作为视频反推编排层，不直接承担复杂算法。

### 6.3.2 主要接口

```python
async def reverse_prompt(
    video_url: str,
    *,
    prompt: str | None,
    user_id: int | None,
    frame_strategy: str = "scene_detect",
    frame_count: int = 5,
    max_scenes: int = 8,
    scene_threshold: float = 0.28,
    sample_fps: float = 2.0,
    prompt_style: str = "seedance",
    vision_channel_id: int | None = None,
) -> VideoReverseResult:
    ...
```

### 6.3.3 输出模型

```python
class VideoReverseResult(BaseModel):
    text: str
    prompt: str
    frames: list[str]
    timeline: list[TimelineItem]
    scenes: list[SceneOut]
    raw: dict[str, Any] = Field(default_factory=dict)
```

## 6.4 `scene_expand.py`

### 6.4.1 职责

把 scenes 转换为画布节点和边。

### 6.4.2 主要接口

```python
def expand_scenes_to_nodes(
    graph: dict[str, Any],
    *,
    source_node_id: str,
    mode: str = "silent",
    create_images: bool = False,
    create_tts: bool = False,
    create_subtitles: bool = False,
    layout: str = "horizontal",
) -> dict[str, Any]:
    ...
```

### 6.4.3 节点生成规则

每个 scene 至少生成：

```text
TextAsset(scene prompt)
ImageToVideo(scene clip)
```

如果 `create_images=true`：

```text
TextAsset → TextToImage → ImageToVideo
```

如果 `create_tts=true`：

```text
TextAsset(narration) → TtsSpeak
```

多个 clip 最终接入：

```text
VideoMux
```

如果有 TTS：

```text
VideoMux + TtsSpeak → MixAudio
```

如果有字幕：

```text
MixAudio 或 VideoMux → SubtitleBurn
```

### 6.4.4 幂等设计

避免重复点击生成大量重复节点。

方案：

- 新节点写入 `source_reverse_node_id`。
- 新节点写入 `source_scene_id`。
- 再次展开时检测已有同源节点。
- 默认提示用户覆盖 / 跳过 / 追加。

第一版可采用“追加”，但节点 label 标明批次。

## 6.5 `asr.py`

### 6.5.1 职责

音视频转文字。

### 6.5.2 主要接口

```python
async def transcribe(
    media_url: str,
    *,
    user_id: int | None,
    language: str | None = "zh",
    channel_id: int | None = None,
) -> TranscribeResult:
    ...
```

### 6.5.3 输出模型

```python
class TranscriptSegment(BaseModel):
    start: float
    end: float
    text: str

class TranscribeResult(BaseModel):
    text: str
    segments: list[TranscriptSegment]
    srt: str
    raw: dict[str, Any] = Field(default_factory=dict)
```

## 6.6 `asset_versions.py`

### 6.6.1 职责

记录和查询所有生成资产版本。

### 6.6.2 主要接口

```python
def record_asset_version(
    *,
    db: Session,
    user_id: int,
    project_id: int,
    node_id: str,
    node_type: str,
    kind: str,
    url: str | None = None,
    text: str | None = None,
    channel_id: int | None = None,
    model_provider: str | None = None,
    model_name: str | None = None,
    prompt: str | None = None,
    params_json: dict[str, Any] | None = None,
    cost: float | None = None,
    status: str = "succeeded",
    error_message: str | None = None,
) -> AssetVersion:
    ...
```

## 6.7 `model_capabilities.py`

### 6.7.1 职责

统一读取、合并和校验模型能力。

### 6.7.2 能力来源

优先级：

1. `Channel.config_json.capabilities`。
2. provider 默认能力表。
3. 系统 fallback。

### 6.7.3 主要接口

```python
def get_channel_capabilities(channel: Channel) -> ModelCapabilities:
    ...

def validate_node_params_against_capabilities(
    node_type: str,
    data: dict[str, Any],
    capabilities: ModelCapabilities,
) -> list[str]:
    ...
```

## 7. API 设计

## 7.1 场景展开 API

```text
POST /api/workflows/{workflow_id}/expand-scenes
```

请求：

```json
{
  "source_node_id": "node_video_reverse_001",
  "mode": "with_image",
  "create_images": true,
  "create_tts": false,
  "create_subtitles": false,
  "layout": "horizontal"
}
```

响应：

```json
{
  "workflow_id": 1,
  "graph_json": {},
  "created_node_ids": ["node_scene_001_prompt", "node_scene_001_t2i"],
  "created_edge_ids": ["edge_001"]
}
```

## 7.2 素材历史 API

### 查询

```text
GET /api/projects/{project_id}/asset-versions
```

查询参数：

```text
kind=image|video|audio|text|prompt
node_type=TextToImage
favorite=true
status=succeeded
limit=50
offset=0
```

响应：

```json
{
  "items": [],
  "total": 0
}
```

### 收藏 / 取消收藏

```text
PATCH /api/asset-versions/{id}
```

请求：

```json
{
  "favorite": true
}
```

### 批量删除

```text
POST /api/asset-versions/bulk-delete
```

请求：

```json
{
  "ids": [1, 2, 3]
}
```

### 发送到画布

```text
POST /api/asset-versions/{id}/send-to-canvas
```

请求：

```json
{
  "position": { "x": 120, "y": 240 }
}
```

## 7.3 Channel Capabilities API

```text
GET /api/channels/{id}/capabilities
```

响应：

```json
{
  "channel_id": 1,
  "kind": "image",
  "provider": "openai-compatible",
  "model": "gpt-image-1",
  "capabilities": {
    "supports_text_to_image": true,
    "supports_image_to_image": true,
    "supports_negative_prompt": false,
    "supports_seed": false,
    "sizes": ["1024x1024"]
  }
}
```

## 8. 数据库设计

## 8.1 `asset_versions`

```sql
CREATE TABLE asset_versions (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  user_id INTEGER NOT NULL,
  project_id INTEGER NOT NULL,
  workflow_id INTEGER,
  node_id TEXT NOT NULL,
  node_type TEXT NOT NULL,
  kind TEXT NOT NULL,
  url TEXT,
  thumbnail_url TEXT,
  text TEXT,
  model_provider TEXT,
  model_name TEXT,
  channel_id INTEGER,
  prompt TEXT,
  params_json JSON,
  cost REAL,
  status TEXT NOT NULL DEFAULT 'succeeded',
  error_message TEXT,
  favorite BOOLEAN NOT NULL DEFAULT 0,
  created_at DATETIME NOT NULL,
  updated_at DATETIME NOT NULL
);
```

索引：

```sql
CREATE INDEX ix_asset_versions_project_created ON asset_versions(project_id, created_at);
CREATE INDEX ix_asset_versions_node ON asset_versions(project_id, node_id);
CREATE INDEX ix_asset_versions_kind ON asset_versions(project_id, kind);
CREATE INDEX ix_asset_versions_favorite ON asset_versions(project_id, favorite);
```

## 8.2 `model_capabilities` 可选表

第一版不强制建表，先放在 `Channel.config_json`。

后续表结构：

```sql
CREATE TABLE model_capabilities (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  provider TEXT NOT NULL,
  model TEXT NOT NULL,
  kind TEXT NOT NULL,
  capabilities_json JSON NOT NULL,
  created_at DATETIME NOT NULL,
  updated_at DATETIME NOT NULL
);
```

## 9. 前端设计

## 9.1 类型层

修改：

```text
frontend/src/workflow/types.ts
```

新增类型：

```typescript
export type Scene = {
  id: string;
  index: number;
  title: string;
  start_time?: number;
  end_time?: number;
  frame_url?: string;
  score?: number;
  analysis: string;
  prompt: string;
  seedance_prompt?: string;
  midjourney_prompt?: string;
  jimeng_prompt?: string;
  narration?: string;
  negative_prompt?: string;
};

export type TimelineItem = {
  index: number;
  start_time: number;
  end_time: number;
  frame_url: string;
  score: number;
};
```

扩展 `WfNodeType`：

```typescript
| "ImageCompare"
| "SpeechToText"
```

扩展 `WfData`：

```typescript
scenes?: Scene[];
timeline?: TimelineItem[];
segments?: Array<{ start: number; end: number; text: string }>;
srt?: string;
before_url?: string;
after_url?: string;
selected?: "before" | "after";
compare_mode?: "slider" | "side_by_side";
```

## 9.2 端口层

修改：

```text
frontend/src/workflow/ports.ts
```

新增：

```typescript
ImageCompare: {
  inputs: [
    { id: "before", kind: "image" },
    { id: "after", kind: "image" }
  ],
  outputs: [
    { id: "image", kind: "image" }
  ]
}

SpeechToText: {
  inputs: [
    { id: "media", kind: "video" },
    { id: "audio", kind: "audio" }
  ],
  outputs: [
    { id: "text", kind: "text" },
    { id: "srt", kind: "text" }
  ]
}
```

## 9.3 模板层

修改：

```text
frontend/src/workflow/templates.ts
```

新增 palette：

```text
图像对比 ImageCompare
口播提取 SpeechToText
```

默认数据：

```typescript
ImageCompare: {
  kind: "image",
  compare_mode: "slider",
  selected: "after"
}

SpeechToText: {
  kind: "text",
  language: "zh"
}
```

## 9.4 节点渲染层

建议拆分组件：

```text
frontend/src/workflow/canvas/FrameGridView.tsx
frontend/src/workflow/canvas/SceneTimelineView.tsx
frontend/src/workflow/canvas/ImageCompareView.tsx
frontend/src/workflow/canvas/TranscriptPreview.tsx
```

`MediaNode.tsx` 只负责分发：

```text
VideoReversePrompt → FrameGridView + SceneTimelineView
ImageCompare → ImageCompareView
SpeechToText → TranscriptPreview
```

## 9.5 检查器层

`WfInspector.tsx` 新增配置区：

### VideoReversePrompt

- 视频上传。
- `frame_strategy`。
- `max_scenes`。
- `scene_threshold`。
- `sample_fps`。
- `prompt_style`。
- `vision_channel_id`。
- 按钮：从分镜生成工作流。

### ImageCompare

- 对比模式。
- 当前选择。
- 交换 before / after。
- 发送选中图到下游。

### SpeechToText

- 输入媒体预览。
- 语言。
- ASR 渠道。
- transcript 编辑区。

### TextToImage

- size。
- seed。
- negative prompt。
- batch size。
- image strength。

### ImageToVideo

- first image。
- last image。
- style image。
- character image。
- product image。
- reference strength。

## 10. 工作流执行器设计

## 10.1 NODE_TYPES 与 PRODUCER_TYPES

新增：

```python
NODE_TYPES.add("ImageCompare")
NODE_TYPES.add("SpeechToText")
PRODUCER_TYPES.add("ImageCompare")
PRODUCER_TYPES.add("SpeechToText")
```

## 10.2 `_has_usable_output`

新增判断：

```text
ImageCompare: data.url 存在
SpeechToText: data.text 或 data.srt 存在
VideoReversePrompt: data.prompt 或 data.scenes 存在
```

## 10.3 `_tag_ports`

新增输出标记：

```text
ImageCompare.image → kind=image, url=data.url
SpeechToText.text → kind=text, text=data.text
SpeechToText.srt → kind=text, text=data.srt
VideoReversePrompt.scenes → kind=scenes, scenes=data.scenes
VideoReversePrompt.frames → kind=image[], frames=data.frames
```

## 10.4 新增执行函数

```python
async def _exec_image_compare(...):
    ...

async def _exec_speech_to_text(...):
    ...
```

增强：

```python
async def _exec_video_reverse(...):
    ...
```

## 10.5 端口校验

新增统一校验：

```python
def validate_edge_by_contract(
    source_type: str,
    source_handle: str | None,
    target_type: str,
    target_handle: str | None,
) -> bool:
    ...
```

调用位置：

- Agent `connect` 工具。
- `scene_expand.py`。
- 工作流运行预检。

## 11. 计费与确认卡设计

## 11.1 通用收费节点

收费节点包括：

- `TextToImage`
- `ImageToVideo`
- `TtsSpeak` 可选
- `SpeechToText` 可选
- 未来其他模型节点

## 11.2 运行状态机

```text
idle
→ running_precheck
→ pending_confirm
→ confirmed
→ running_provider
→ succeeded
```

失败路径：

```text
running_provider
→ failed
→ refund_if_charged
```

取消路径：

```text
pending_confirm
→ cancelled
```

## 11.3 幂等字段

每次收费运行需要：

```text
run_id
node_id
workflow_id
user_id
estimated_cost
confirmed_at
charged_transaction_id
refund_transaction_id
status
```

如果当前项目已有视频确认卡结构，应复用，不新增平行系统。

## 11.4 TextToImage 确认逻辑

```text
_exec_t2i
→ estimate_cost
→ if cost > 0 and not confirmed: raise ConfirmRequired
→ charge
→ image_gen.generate
→ record asset version
→ on failure refund
```

## 12. Agent / MCP 设计

## 12.1 新增 MCP 工具

```text
expand_scenes_to_nodes
run_image_compare
run_speech_to_text
```

### expand_scenes_to_nodes

输入：

```json
{
  "source_node_id": "node_001",
  "mode": "with_image"
}
```

输出：

```json
{
  "created_node_ids": [],
  "created_edge_ids": []
}
```

### run_image_compare

输入：

```json
{
  "target_id": "node_image_compare_001"
}
```

### run_speech_to_text

输入：

```json
{
  "target_id": "node_stt_001"
}
```

## 12.2 Skill 约束

`seedance-tvc` Skill 应追加：

```text
如果有参考视频：
1. 先创建 VideoReversePrompt。
2. 运行反推。
3. 读取 scenes。
4. 如果 scenes 数量 1–4，调用 expand_scenes_to_nodes。
5. 如果 scenes 超过 4，先询问用户是否压缩。

如果有产品图：
1. 作为 TextToImage 的参考图。
2. 作为 ImageToVideo 的产品参考图。
3. 禁止改变产品 logo、瓶身、颜色。

如果需要扣费：
1. 等待用户确认。
2. 不自动绕过确认卡。
```

## 13. 素材历史设计

## 13.1 记录策略

成功生成后记录：

```text
TextToImage → image asset
ImageToVideo → video asset
TtsSpeak → audio asset
VideoMux → video asset
SubtitleBurn → video asset
SpeechToText → text asset
VideoReversePrompt → prompt asset + frame image assets
```

失败时可选记录：

```text
status=failed
error_message=...
```

## 13.2 发送到画布

根据 kind 创建节点：

```text
image → ImageAsset
video → VideoAsset
audio → AudioAsset
text / prompt → TextAsset
```

新节点 data：

```json
{
  "label": "历史素材",
  "kind": "image",
  "url": "/uploads/1/xxx.png",
  "source_asset_version_id": 123
}
```

## 13.3 删除策略

第一版建议：

- 删除 `asset_versions` 记录，不物理删除文件。
- 后续增加“清理未引用文件”后台任务。

这样避免用户删除历史导致画布已有节点失效。

## 14. 模型能力矩阵设计

## 14.1 Capability Schema

```typescript
type ModelCapabilities = {
  kind: "llm" | "image" | "video" | "tts" | "asr";
  vision?: boolean;
  supports_text_to_image?: boolean;
  supports_image_to_image?: boolean;
  supports_multi_reference?: boolean;
  supports_negative_prompt?: boolean;
  supports_seed?: boolean;
  supports_text_to_video?: boolean;
  supports_image_to_video?: boolean;
  supports_first_last_frame?: boolean;
  supports_style_reference?: boolean;
  supports_character_reference?: boolean;
  supports_product_reference?: boolean;
  supports_transcription?: boolean;
  sizes?: string[];
  aspects?: string[];
  duration_min?: number;
  duration_max?: number;
  provider_params?: Record<string, unknown>;
};
```

## 14.2 默认能力

OpenAI-compatible image 默认：

```json
{
  "kind": "image",
  "supports_text_to_image": true,
  "supports_image_to_image": true,
  "supports_negative_prompt": false,
  "supports_seed": false,
  "sizes": ["1024x1024"]
}
```

Seedance video 默认：

```json
{
  "kind": "video",
  "supports_image_to_video": true,
  "supports_text_to_video": false,
  "supports_first_last_frame": false,
  "duration_min": 4,
  "duration_max": 10,
  "aspects": ["16:9", "9:16", "1:1"]
}
```

LLM vision 默认：

```json
{
  "kind": "llm",
  "vision": true
}
```

## 15. 错误处理设计

## 15.1 节点错误格式

节点失败时写入：

```json
{
  "status": "failed",
  "error": "错误摘要",
  "error_detail": "可选详细信息"
}
```

## 15.2 用户可理解错误

错误应转为用户可理解文案：

- `ffmpeg not found` → “未检测到 ffmpeg，请先安装或配置 ffmpeg。”
- `vision channel not found` → “未配置可读取图片的 LLM 渠道，已使用文本模拟分析。”
- `insufficient balance` → “余额不足，无法继续生成。”
- `unsupported capability` → “当前模型不支持首尾帧输入，请更换模型或移除尾帧。”

## 15.3 fallback 策略

```text
scene_detect 失败 → fixed frames
vision LLM 失败 → text LLM
text LLM 失败 → local fallback scenes
ASR 失败 → 节点失败，不生成字幕
ImageCompare 缺一张图 → 节点等待输入，不失败
```

## 16. 安全设计

## 16.1 上传文件安全

- 只允许读取 `/uploads` 内文件。
- `local_upload_path` 必须防路径穿越。
- 视频抽帧临时目录按 run_id 隔离。
- 临时文件定期清理。

## 16.2 API Key 安全

- 前端不返回明文 Key。
- Channel 测试接口只返回成功 / 失败。
- 日志不得打印 Authorization header。

## 16.3 Agent 权限

Agent 工具必须受当前用户和项目约束：

- 不能访问其他用户项目。
- 不能读取其他用户上传文件。
- 不能绕过确认卡。

## 17. 性能设计

## 17.1 视频抽帧性能

约束：

- 默认 `sample_fps=2`。
- 默认最多输出 8 个 scenes。
- 抽帧临时图缩放到小尺寸用于差异计算。
- 只保存最终关键帧，不保存所有采样帧。

## 17.2 多模态请求控制

- 一次最多发送 8 张关键帧。
- 超过时按 scene score 选 Top N。
- 图片压缩到合理尺寸。
- 请求超时可配置。

## 17.3 前端渲染

- 关键帧缩略图懒加载。
- 历史素材分页。
- 大文本折叠显示。

## 18. 测试设计

## 18.1 单元测试

覆盖：

- `video_scene_detect.detect_scenes`。
- scene boundary 合并逻辑。
- `vision_llm` JSON 解析。
- `scene_expand` 节点生成。
- `validate_edge_by_contract`。
- `ImageCompare` 输出选择。
- `model_capabilities` 校验。

## 18.2 集成测试

链路一：

```text
VideoAsset → VideoReversePrompt → scenes → expand_scenes_to_nodes
```

链路二：

```text
TextToImage → ImageCompare → ImageToVideo
```

链路三：

```text
VideoAsset → SpeechToText → SubtitleBurn
```

链路四：

```text
TextToImage 收费确认 → 成功扣费 / 失败退款
```

## 19. 实施拆分建议

### 19.1 第一批实现

目标：把视频反推做实。

- `video_scene_detect.py`
- `vision_llm.py`
- 增强 `video_reverse.py`
- 增强 `VideoReversePrompt` 前端展示

### 19.2 第二批实现

目标：分镜自动搭图。

- `scene_expand.py`
- `expand_scenes_to_nodes` MCP 工具
- 前端“从分镜生成工作流”按钮
- 端口校验

### 19.3 第三批实现

目标：首帧图生成和对比。

- `TextToImage` 确认卡和计费
- `ImageCompare` 节点
- 图像参数面板

### 19.4 第四批实现

目标：生成历史和复用。

- `asset_versions` 表
- 资产版本记录服务
- 历史素材 Tab
- 发送历史素材到画布

### 19.5 第五批实现

目标：声音字幕与模型能力。

- `SpeechToText`
- ASR provider
- `ImageToVideo` 首尾帧扩展
- `model_capabilities`

## 20. 设计验收标准

完成本文档设计后，系统应满足：

1. `VideoReversePrompt` 能输出 `frames`、`timeline`、`scenes`、`prompt`。
2. `scenes` 能一键展开为多镜头工作流。
3. `TextToImage` 支持收费确认和失败退款。
4. `ImageCompare` 能比较两张图并输出选中图片。
5. 生成图 / 视频 / 音频 / 文本能进入素材历史。
6. `SpeechToText` 能从视频或音频生成文本和 SRT。
7. 前端检查器能根据模型能力显示或隐藏参数。
8. Agent 只能通过 MCP 工具调用这些能力，不绕过节点执行器。
9. 端口校验能防止 Agent 或自动展开服务生成非法边。
10. 所有新增能力均能通过手动画布和 Agent 两种路径使用。

## 21. 最终系统形态

实现完成后，SeeMeTVC 的核心能力链路为：

```text
产品图 + 参考视频 + Brief
→ VideoReversePrompt 智能反推
→ SceneExpand 自动搭图
→ TextToImage 生成首帧
→ ImageCompare 选择最佳首帧
→ ImageToVideo 生成镜头
→ SpeechToText / TtsSpeak 处理口播
→ VideoMux / MixAudio / SubtitleBurn 合成成片
→ AssetVersions 沉淀所有创作资产
```

这套设计让 SeeMeTVC 不只是补齐 Tapnow Studio 的功能短板，而是形成更适合美妆广告生产的 Agent 化、平台化、可计费、可追踪的创作系统。
